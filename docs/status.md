# Project status & wiring

> ## ⚠️ Read this first
>
> Ironclad's engine grew out of a **proven, in-production orchestrator** (years of
> daily use). It is currently undergoing a **complete redesign**: the original
> single-process CLI is being split into a headless **server** + thin **client**,
> containerized, and given a reasoning-worker fan-out and a purpose-built TypeScript terminal client.
>
> **Not everything is re-wired or re-tested yet.** The pieces below are marked
> honestly: *proven* (inherited, battle-tested), *wired + tested* (rebuilt and
> verified in the new architecture), *placeholder* (hook exists, backend/logic not
> yet ported), or *opt-in* (off by default). Treat `main` as a development snapshot.

This document is the single source of truth for **what actually works right now**.

> **Release model.** Ironclad is **pre-release** (`0.0.x`, alpha). Tagged releases are
> published on **PyPI** as `ironclad-ai` and as **GitHub Releases** (latest `v0.0.26`);
> the importable wheel is the Agent-Contract-Kernel (`ack` + `ack.lodestar`), while the
> orchestration engine ships as runnable scripts until its API stabilizes. There is **no
> stable/1.0 release yet** — APIs, layout, and config may change between `0.0.x` versions.

## Module reference

| File | What it does |
|------|--------------|
| `engine/gx10.py` | The orchestrator **engine library** (the *proven* core). Agent loop, tool execution, deterministic `TaskStore`, fail-closed macros (`stage_handover` / `advance_pipeline` / `launch_coder` — the orchestrator starts a staged handover on demand, autopilot off / `record_design` + the design→impl approval gate — an implementation handover is refused until the design is recorded + `/approve`d, no blind coding; opt-in `design_gate.enabled`, default off/byte-identical / the central task board `/board` + `BOARD.md` — an LLM-free pending/in_progress/done projection of the TaskStore, kept current fail-soft / the basic no-blind-advance gate — an advance to `done` requires a feedback `status: done`, opt-in `advance_gate.enabled`, default off/byte-identical / the watcher/autopilot disentangle (`automation.decoupled` — autopilot self-sufficient, no contradictory double message) + a task-scoped detect-progress heartbeat (`heartbeat.stall_seconds`) + an explicit BLOCKED task annotation surfaced on the board / the code-subdir software isolation — `paths.code_subdir` routes model/coder execution under `<project>/<subdir>` (e.g. `src/`) so the product tree is separated from the control-plane, opt-in/default-off), config-tree loader, platform detection, context trimming. The server imports it; the standalone monolithic CLI was **removed** (one way: server + client). |
| `engine/server.py` | **Headless server** (new). Drives the engine with no UI, exposes plain-HTTP endpoints, serializes agent access, runs the feedback-side reconciler. |
| `clients/ink/` | **Recommended terminal client** (new, TypeScript). Purpose-built renderer (own React reconciler + flexbox + packed cell buffer + cell-level diff) → **ghost-free resize**, smooth streaming, native scrollback/selection/copy. Slash-command autocomplete, local `!cmd` shell, in-CLI `/update`, per-project session. Same HTTP/tool-bridge contract; build-from-source (Node ≥ 22). |
| `engine/client.py` | **Thin client (legacy)**. Line REPL + the local code-agent pool (`claude --print`) that keeps project code on your machine. Zero-dependency fallback. |
| `engine/tui.py` | **Full-screen client (legacy)**. prompt_toolkit UI, live streaming, status toolbar, scrollback, compressed multi-line paste. |
| `engine/commands.py` | Shared command router for REPL + TUI (`/command` → local or forwarded to the server). |
| `engine/workers.py` | **Reasoning-worker fan-out** (new). Independent prompts run concurrently against the model (`POST /fanout`). |
| `ack/` | **Agent-Contract-Kernel**: schema-as-SSOT (`case_spec`), bounded validate→reask (`validated_emit`), constrained emission (`constrained_emission`), the shared failure-classification taxonomy (`failure_class`), the failure→action policy (`strategy`), per-TaskType loop budgets (`loop_profile`), the mark-only evaluation layer (`verify`), the output-quality circuit breaker (`quality`), the process-self-correction policy (`process`), the Loop-Intelligence Hook-Bus (`hooks`), registry, doctor, generator, the opt-in `lodestar` plugin. |

## Wiring status

| Component | Status | Notes |
|-----------|--------|-------|
| Agent loop, tools, TaskStore, macros | **proven** | Inherited from the in-production orchestrator. |
| Config tree + `GX10_*` env + language setting | **wired + tested** | `language` (reply language) default `en`. |
| **Runtime config control** (`/config get\|set <dotted.key> <value>`) | **wired + tested** | Generic, plugin-agnostic read/override of the live config tree without a restart: `set` writes the dotted key and re-derives engine globals via `_apply_config`; keys core doesn't model (e.g. plugin sections) are stored and re-read by their owner on next use. Process-local (not persisted). See [`config-runtime.md`](config-runtime.md). |
| **Setup types** (`setup.type`, boot-fixed operating mode) | **wired + tested** | A boot-only, runtime-frozen config key selecting where ironclad runs — orchestrator + agents always co-located (no cross-machine offload): `server` (engine code default — everything on the model host, in-engine only, byte-identical) or `local` (engine + agents native on the user's machine; the model + **Cold** memory (Mem0) stay remote, the **Warm** cache (Valkey) follows the orchestrator — #385; offload = local subprocess, requires a remote `base_url` + a reachable CLI), plus `auto` (the desktop launcher default, INSTALL-1) which derives the topology from `base_url` at boot — loopback ⇒ `server`, remote ⇒ `local` — so a fresh default install boots without baking a model host into the repo. Fail-closed on a mode that can't be honored; `sealed` forces `server` (no egress). See [`setup-types.md`](setup-types.md). |
| **Orchestrator system prompt** (`engine/prompts/`, `GX10_PROMPT`) | **shipped** | A generic, secret-free default orchestrator system prompt ships in `engine/prompts/GX10_Orchestrator_SystemPrompt.md` (role, tool-use discipline, the fail-closed macros, the workflow). A deployment overrides it with a project-specific prompt via `GX10_PROMPT` (kept private, never in `core/`). |
| **Orchestrator version stamp** (`orchestrator_version`) | **wired + tested** | The engine reads an opaque build identity — `GX10_ORCHESTRATOR_VERSION` → else `engine/VERSION` (gitignored) → else `"unknown"` — and exposes it in `/health` and the boot line. Core only reads and shows it (no git/SHA logic); a deployment stamps the same value into every target so co-located builds are verifiably drift-free. |
| **Provider-router / dispatcher** (`engine/providers.py`, `router.py`, `dispatch.py`) | **wired + tested — off in `server`** | Backend registry + routing policy (effort·sovereignty·load·budget) + governed dispatch that wraps `parallel_reason` with fail-soft spill. Boot-derived from `setup.type`: **disabled in `server`** (default — `providers_enabled=False`, byte-identical in-engine path), enabled in `local`. Off by default; deterministic unit tests (`test_dispatch`, `test_router`, `test_providers`, `test_artifact_routing`). **First-class `web_search`** (epic #505): a strict `{query, allowDomains?, blockDomains?}` tool (validated in a pure validator; schema grammar-clean) running through a **vendor-neutral adapter seam** (`search.adapter`: `cli` delegate · `brave` **native HTTP** on stdlib `urllib`, **local-only** · `mock`) **independent of this lane**, so a native-search deployment with no CLI provider still offers it. Returns structured results + `durationMs`, capped, with a **deterministic `Sources:`** block; a `[search]` stream sentinel drives a "web N · Xms" footer chip in every client. **Trust-gated**: blocked under the `sealed` profile (operator opt-in `security.web_in_sealed`), enforced at the offer- AND exec-gate, and kept out of `LOCAL_TOOL_NAMES`. A conservative EN+DE current-info classifier proactively steers "latest/today/aktuelle Lage" requests to it, and a **fail-closed `execute_command` guardrail** refuses a remote/web fetch or an unbounded/progress-emitting process (redirecting web fetches to `web_search`) + hardens PowerShell with `$ProgressPreference='SilentlyContinue'`. The search key (`GX10_SEARCH_API_KEY`) is name-indirected from the environment; the optional CLI backend is a private `conf/` detail. See [`web-search.md`](web-search.md). **SOFT distinct-reviewer anti-affinity** (#457): `RouteRequest.excluded_provider_ids` (caller-passed — `route_one` stays pure) drops the artifact's producer from the candidates so a review-of-a-review never routes back to its author while an equal peer remains; if excluding it would leave no capable agent the producer is kept (waived, never declined). `RouteDecision.distinct_reviewer` records `applied`/`waived`/`None`; HARD sovereignty/budget outrank it (a SENSITIVE request stays local rather than leaking to a "distinct" external). A private CI invariant `review-distinct-reviewer` asserts the seam stays intact. |
| Server/client split (`/chat`, `/chat/stream`, `/tasks`, `/pending`, `/feedback`, `/tool-result`, `/cancel`, `/doctor`, `/session/*`) | **wired + tested** | Verified PC→LAN→Spark; headless capture; streaming; `/tool-result` is the local code-tool passthrough. |
| Thin client + parallel code-agent pool | **wired + tested** | Bounded pool; claim-once; unclaim-on-failure (in both the TypeScript and Python clients). |
| **TypeScript terminal client** (`clients/ink/`) | **wired + tested** | Purpose-built renderer (React reconciler + Yoga flexbox + packed cell buffer + cell-level diff). **419 passing** `node:test` cases (423 total, 4 skipped) across renderer/UI/net/tools; ghost-free resize, native scrollback/selection/copy, live-streaming markdown with preserved + syntax-highlighted code, slash-command autocomplete, `!cmd`, in-CLI `/update`, per-project session, the bridged `execute_command` carrying the same deterministic `N directories, M files` listing count AND the machine `AnswerData:` payload the server renders into the localized ready-made `Answer:` reply sentence (fs-computed, symlink-following, #1195/#1202), coloured listing output preserved through the tool-block parser (native SGR kept for the renderer, #1196), and the **Phase-d session handshake** under the `sealed` profile (open + heartbeat + re-open-on-loss + close on exit, mirroring `client.py`; unit-tested); adversarially verified. Build-from-source / global install (Node ≥ 22). |
| Full-screen Python TUI (stream, toolbar, scroll, paste) — **legacy** | **wired + tested** | Render/scroll/paste unit-checked; runs in a real terminal. Superseded by the TypeScript client. |
| Reasoning-worker fan-out (`/fanout`) + `parallel_reason` tool | **wired + tested** | Stateless concurrent reasoning, **GPU-safety-governed**: a config-driven concurrency cap **and** a token-budget envelope (`concurrency × max_tokens ≤ max_batch_tokens`) so a large `max_tokens` lowers parallelism instead of over-subscribing the box — overflow just queues. Core ships conservative defaults (concurrency 4); the deploy pins model-matched values (8 = our model's `max_num_seqs`). Now also exposed **in-engine** as the `parallel_reason` tool, so the orchestrator itself batches independent items — **server-only** (it needs the server-constructed governed workers; in a plain library/CLI import it reports "unavailable"). Live-verified: governed `/fanout` 8/8 at ~5.8× speedup on the reference GPU. |
| **Runtime self-telemetry** (`engine/telemetry.py` + `GET /metrics`, epic #1059/#1060) | **wired + tested** | A bounded, thread-safe rolling record of per-generation latency, token cost, and errors, recorded at the generation site (skips user-aborts, fail-soft — never breaks a turn). A token-gated **`GET /metrics`** returns the all-time + recent-window aggregate (turns, error rate, latency p50/p95, prompt/completion/total tokens) plus an **SLO verdict** (config-tunable `metrics.slo_error_rate` / `slo_p95_latency_s` / `window_s`) and a **recent-vs-baseline anomaly** signal — so a degradation in an unattended deployment is observable/scrapable without a human at the console. stdlib-only. An **alerting pipeline** (`engine/alerting.py`, #1061) turns that SLO/anomaly verdict into deploy-correlated alerts (SLO breach → critical, anomaly → warning) and **pages** them to the configured webhook (#1083): a gated periodic self-scan (`alert.enabled` + a webhook → a server daemon every `alert.interval_s`) fires them automatically, and a token-gated **inbound `POST /alert`** receiver pages an external monitor's alert. Default-off (no scanner thread when off); fail-soft. |
| **Per-action audit ledger** (`engine/audit_ledger.py`, epic #1043/#1084) | **wired + tested — default OFF** | A tamper-evident, hash-chained (`sha256(seq\|prev_hash\|payload)`) record of the orchestrator's mutating tool actions (`write_file`/`write_last_reply`/`edit_file`/`execute_command`/`create_issue`) under `state_root()/audit/ledger.jsonl`; `verify_chain` detects any edit / reorder / middle-deletion. **Content-free** (actor + action + target + ok, never the file body or command output) and **opt-in** (`audit.enabled` / `GX10_AUDIT_ENABLED` — no records when off, byte-identical); fail-soft. Core-owned (not the private dev-process ledger). **Full surface + who/what/when/why + tamper-resistance (#1067):** `audit.scope: all` records EVERY tool call (not just the mutating subset), each record carries who (actor) / what (action + target) / when (ts) / why (`reason` = active scope); and `write_file`/`edit_file` **refuse any path under the audit dir** (`_is_audit_path`) so the agent can't tamper with its own trail — tamper-RESISTANCE on top of the hash-chain's tamper-EVIDENCE (append-only OS enforcement / a WORM sink is remaining scope). |
| **Per-principal RBAC + multi-tenant memory (foundation)** (`ack.authz`, epic #1065/#1071) | **wired + tested — default OFF** | The pure foundation for multi-user/enterprise on top of today's single-tenant model: a `Principal` (id/role/tenant), a deny-by-default `DEFAULT_ROLES` policy (role → allowed danger-tiers — admin=all · operator/agent=all-but-destructive · reader=read_only), `authorize(role, tier)`, `resolve_principal(token, principals)` (secret-free — token values from env), and `tenant_scope(scope, tenant)` for isolated multi-tenant memory (default tenant = no-op). Gated `security.multi_tenant` / `GX10_MULTI_TENANT` (default off, byte-identical); engine seam `_authorize_action`/`_tenant_mem_scope`. Foundation only ([ADR-0014](adr/0014-rbac-multi-tenant.md)) — full request-path authz · ABAC · memory-service tenant enforcement · per-tenant routing are remaining scope (Enterprise milestone #20). |
| **OS exec sandbox** (`engine/sandbox.py`, epic #1065/#1069) | **wired + tested — default OFF** | `execute_command` ran agent-generated shell with the orchestrator's privileges; now it can be wrapped in an unprivileged OS isolation backend (bubblewrap / firejail) when configured + on PATH — the foundational win is **network isolation** (`--net=none` / `--unshare-net`: no exfiltration / no C2), filesystem stays accessible. `available_backend` (PATH) + `wrap_command` (pure); wired into the POSIX branch via `security.sandbox` / `GX10_SANDBOX` = off\|auto\|bwrap\|firejail. Never claims containment it can't provide (no backend → runs as-is). Defense-in-depth ([ADR-0013](adr/0013-exec-sandbox.md)) — full FS isolation · seccomp · container runtime · Windows containment are remaining scope. |
| **Ingestion prompt-injection defense** (`ack.injection`, epic #1065/#1068) | **wired + tested — default OFF** | A layered, core-owned defense against content-borne steering: a precision-first `scan` for injection patterns (instruction-override · role-switch · role-marker/tag · tool-call injection) + a `wrap_untrusted` trust boundary that fences every ingested result (`read_file`/`search_files`/`list_directory`/`execute_command`/`fetch_url`/`view_issue`) as **data, not instructions** at the #1046 ingestion choke point, warning when injection is detected. Gated `security.injection_defense` / `GX10_INJECTION_DEFENSE` (default off, byte-identical). Defense-in-depth ([ADR-0012](adr/0012-ingestion-injection-defense.md)) — **not** complete (an LLM classifier · per-source trust · output-side checks are remaining scope). |
| **Verbatim page fetch** (`fetch_url` tool, epic #1043/#1074) | **wired + tested — trust-gated** | A bounded `fetch_url(url)` reads a specific http(s) page verbatim (RFCs/standards/API specs/docs) via stdlib `urllib` — `web_search` finds pages, `fetch_url` reads one. Offered only when outbound is allowed (blocked under `sealed` unless `security.web_in_sealed`); **SSRF-guarded** (http/https only; refuses loopback/private/link-local hosts incl. cloud metadata `169.254.169.254`); double-bounded (hard byte cap + the ingestion choke-point char cap). |
| **Forge issue-filing** (`create_issue` tool, epic #1043/#1073) | **wired + tested — default ON (capability-detected)** | A model-callable `create_issue(title, body_file, labels?, milestone?)` files an issue in the project's code forge (GitHub via the `gh` CLI), so the orchestrator can track its own work instead of dead-ending at "I have no GitHub tool" (surfaced by the whole-lifecycle capability audit). **Capability-detected** (`_forge_available()`): offered whenever `gh` is on PATH + authenticated — mirroring `web_search`/memory/skills so the tool surface is uniformly capability-detected; installing + authing `gh` IS the operator's deliberate opt-in. The operator can force it off (`forge.enabled=false` / `GX10_FORGE_ENABLED=0`), and it is blocked under the `sealed` profile (no autonomous outbound writes, like `web_search`/`fetch_url`). **secret-free** (ambient `gh` auth, no token on the wire, no repo literal in core; optional `forge.repo`); **escape-free** (the body comes from a FILE the model wrote via `write_last_reply`, never a giant JSON arg). |
| **Forge issue-reading** (`view_issue` tool, epic #1043) | **wired + tested — default ON (capability-detected)** | The READ counterpart to `create_issue`: `view_issue(number)` reads a tracker issue (number/state/title/labels/milestone/body/url) so the orchestrator resolves a `#NNN` reference by querying the forge **directly** — not by grepping git history (which only cites issues a merged PR CLOSED, so an OPEN issue is invisible there → the failure that motivated it: an agent grep-flailing `#NNN` and falsely concluding "does not exist"). A non-existent number returns an authoritative `NOT_FOUND` (the tracker **was** queried), never an inference from a missing commit; the body is bounded so a huge issue can't blow the window. Offered together with `create_issue` under the same `_forge_available()` gate (`gh` present, not `sealed`, `forge.enabled`). The default orchestrator system prompt routes a `#NNN` reference to `view_issue` and forbids concluding non-existence from a missing commit. |
| **Forge adapter seam** (`forge_adapters.py` + native `forge_native.py`, #1213) | **wired + tested — default `cli`** | Makes the forge tools **general in ironclad**, not `gh`-on-the-box: a vendor-neutral `forge.adapter` seam mirroring the `web_search` adapter — `cli` (default, the ambient `gh` CLI, **byte-identical**) \| `native` (a stdlib-`urllib` GitHub REST client, so `create_issue`/`view_issue` and the follow-up PR/comment tools work with **no `gh` on PATH**, e.g. the Spark `server` topology — keyed by a **name-indirected** token `forge.token_env` [default `GX10_FORGE_TOKEN`] + `forge.repo`, SSRF-guarded to `api.github.com`) \| `mock`. `_forge_available()` is now transport-aware (`gh` present **OR** a native token+repo configured); still `forge.enabled`-gated and `sealed`-blocked. All GitHub literals are confined to the native module; no repo/token literal in core. |
| **Forge PR-opening** (`create_pr` tool, #1215) | **wired + tested — default ON (capability-detected)** | The WRITE-sibling of `create_issue` on the forge seam: `create_pr(title, body_file, base?, head?, draft?)` opens a PR through the active adapter (`cli` = `gh pr create`; `native` = `POST /pulls`), so the orchestrator's own Issue→Branch→**PR**→Merge loop no longer drops to the raw shell. Escape-free (body from a FILE), **open-only** (it never merges — merge stays a CI/review gate; `merge_pr` is deliberately out of scope). Capability-detected + `sealed`-blocked via `_forge_available()`; audited (`_AUDIT_TOOLS`). The native path requires an explicit `head` (no local git to infer it) and defaults `base` to the repo's default branch. The default system prompt routes "open a PR" → `create_pr`. |
| **Forge issue-commenting** (`comment_on_issue` tool, #1217) | **wired + tested — default ON (capability-detected)** | The third leg of create/read/**comment** on the forge seam: `comment_on_issue(number, body_file)` appends a comment through the active adapter (`cli` = `gh issue comment`; `native` = `POST /issues/{n}/comments`), so the orchestrator's round-trip / status-note flow no longer shells out. Escape-free (body from a FILE), **narrow — comment-only** (never close/reopen/relabel; close is policy-sensitive and excluded). Authoritative `NOT_FOUND` for a missing issue (repo-disambiguated on native). Capability-detected + `sealed`-blocked + audited; the default system prompt routes "record a status/datapoint on an issue" → `comment_on_issue`. |
| **Forge PR CI/mergeability read** (`pr_status` tool, #1219) | **wired + tested — default ON (capability-detected)** | The merge-readiness gate as a first-class read: `pr_status(number)` returns a deterministic per-check verdict (ALL PASSING / N FAILING / N PENDING) + `mergeable`/`mergeStateStatus`/`reviewDecision`, through the active adapter (`cli` = `gh pr checks` + `gh pr view`; `native` = `GET /pulls/{n}` + the head commit's `check-runs` **paginated** + legacy commit `statuses` + a best-effort `reviewDecision` approximated from `/reviews`, so it matches `gh pr checks` on the no-`gh` topology). The load-bearing subtlety: `gh pr checks` **exits non-zero as DATA** (pending=8, fail=1), so the cli adapter parses stdout first, never treating the exit code as an error. A **SNAPSHOT** — non-blocking, never a `--watch`/poll (the single agent-lock model; re-poll across turns). A read → in `_INGESTION_TOOLS` (injection-fenced + char-capped, unlike the write tools), **not** `_AUDIT_TOOLS`; authoritative `NOT_FOUND` for a missing PR. Collapses the #1211 audit's `pr_checks_status` + `view_pr` into one read. |
| **Initiative-centric state layout** (`vault/<slug>/`, `.ironclad/`) | **wired + tested** | Engine machinery is hidden under `state_root` (`.ironclad/`: `session.json`, warm-cache, `active` marker); every produced artifact lives under the **active initiative** `vault/<slug>/` (visible `decisions/ proposals/ reviews/ runs/ tasks/`, hidden `.work/` plumbing). Explicit creation (`/project new <name>`); the **engine-routed** artifacts — the `TaskStore`, the `stage_handover`/`advance_pipeline` plumbing, and MPR `runs_dir` — resolve under the active initiative (**fail-closed** with none active; background scanners soft-skip), while `decisions/`/`proposals/`/`reviews/` are seeded dirs the agent writes into. Project root stays clean. See [`state-and-initiative.md`](state-and-initiative.md). |
| **Project isolation** (`/project`, `/switch`, installation-global registry) | **wired + tested** | Every engine session runs in a *project*. An installation-global **registry** (under `GX10_HOME`, atomic + OS-file-locked) is the SSOT of registered projects + the persisted continuity pointer; the engine's current project is **single-active per process** (cached, never re-read per request, so a second engine can't yank a running one). `/project list\|new <name> [--type] [--path]\|active\|track …` manages projects (the guided setup command, #601 S16: `new` mints a fresh isolated project — root `<cwd>/<slug>` or `--path`, minted `mem_ns`, made active, and `--type` seeds the first vault unit; `delete <id> [--purge]` forgets the project's memory + drops the registry entry [dirs kept unless `--purge`, which is guarded against the cwd/boot/home]; `archive`/`unarchive` toggle a reversible hidden flag [data kept, not switchable, hidden from `list` unless `--all`]; `/initiative` is a deprecated alias); `/switch <project_id>` quiesces and rebinds (and refuses an archived target) — saving the leaving conversation under its own root, binding the target's paths (`state_root`/`vault_root` resolve under the project root) + its own **memory partition** (`mem_ns`), and swapping the conversation (no cross-project bleed). The implicit **`default`** project binds the boot workdir + the legacy/base memory partition, so a single-project install is byte-identical to before. A **registered** project is **fail-closed** at bind (#601 S14-2): a non-`default` project with an invalid/empty `mem_ns` is refused (it would otherwise silently leak into the base partition); boot degrades to the safe `default`, a `/switch` rolls back. The memory handle is a single installation-global connection re-keyed **per call** by the active partition (no rebuild-on-switch). A switch is **refused** while a dev unit is in-flight for either side; the system prompt is project-independent and preserved. The model's **file tools, `execute_command`, and a launched code-agent run with the active project's root as their working directory** (the switch does not `chdir` the process — the exec cwd is threaded through the active context; absolute paths verbatim; `default` = the boot workdir, byte-identical). A project can carry parallel **tracks** (single-active; [ADR-0011](adr/0011-dev-process-rework-project-isolation.md) AD-2', #601 S12a): a non-`main` active track gets its own isolated vault subtree under `.tracks/<track>/` (`vault_root` is track-aware), while the default `main` track is byte-identical to the pre-track layout. Tracks are managed from the CLI with `/project track new|use|list` (#601 S16): `new` creates-and-switches, `use` switches, `list` shows them — a switch rebinds the context so the vault subtree + memory sub-scope + per-track library follow. Vault mutation is **serialized** per project+track (#601 S12b): a `vault_lock(pid, track)` distinct from the dev-loop `project_lock` wraps the vault writers (`initiative_new`/`reconcile_vault` + the macros `_stage_handover`/`_advance_pipeline`) — reentrant within a call stack, OS-serialized cross-process, fail-soft. See [`project-isolation.md`](project-isolation.md). |
| **Dev-process facade** (`ack.devprocess.api`) | **wired + tested** | The single curated, versioned (ADR-0004) public seam a generated per-project tool delegates to — `select_unit`, `stage_handover`, `record_feedback`, `advance`, `deliver`. Dependency-inverted (`set_driver`/`get_driver` + a `runtime_checkable` `DevProcessDriver` protocol): the facade imports nothing from the engine, ships in the wheel (`ack.devprocess`), and degrades cleanly (no driver → a clear `SubstrateUnavailable`, never an `ImportError` — proven by the clean-room smoke). **Live in-engine**: a driver registered at import wires the two engine-owned verbs (`stage_handover`/`advance` → the real impls) and the `stage_handover`/`advance_pipeline` tool calls route through it; `select_unit`/`deliver` (the private dev-loop substrate, which `core/` must not import) and `record_feedback` (the server reconciler inbox) report `SubstrateUnavailable` until wired by their owner. |
| **▾ Loop Intelligence Layer (epics #602 + #855/ACE)** — a reflection loop that learns across runs; the C2 consumers below are *default-off, byte-identical* while the persistence/learning tier is the **always-on ACE core** (#863); the rows below are its components | **wired + tested** | **One coherent layer, not nine disconnected parts.** **Keystone:** the **Hook-Bus** (`ack.hooks`, last row of this group) over which the engine publishes its agent-loop events. **Consumers** (each opt-in per flag, observer-only, **never** gating the fail-closed core): a mark-only **Verifier** scores each handover → a **Quality circuit-breaker** trips on sustained degradation → a **Strategy revisor** turns a *classified* **Failure-class** into a targeted next action. **Persistence + learning (always-on, #863):** the **ACE** evolving-context playbook — the engine `PlaybookStore` — is the loop-intelligence backend that distils + replays what worked, **superseding** the #602 string Lesson + Process-SC consumers. **Budgets:** per-`TaskType` **Loop Profiles** bound the loop. Each **C2 consumer** (Verifier/Quality/Strategy) is **byte-identical when its flag is off**; the ACE persistence tier is always-on. The per-component rows below give the wiring detail. |
| **LessonStore / LessonProvider API** (`ack.lessons`) | **wired + tested** | The curated, versioned (ADR-0004) public delegation seam for scope-partitioned actionable lessons (#601 S14-3 / AD-10) — the stable surface that **unblocks epic #602** (its Distiller registers a provider; lesson semantics live there, not here). `runtime_checkable LessonProvider` (`get_lessons`/`report_lesson`/`brief`) + `set_provider`/`get_provider`; ships in the wheel; imports nothing from the engine. **Fail-soft** with no provider (reads `[]`, writes no-op, a provider error never breaks a turn — lessons are advisory). Scope-priority `brief()` merge (delegated or composed, dedup+limit). **Fail-closed redaction-gated `promote()`** (AD-9): a project-private lesson reaches a broader scope only through an approving redactor. The scope is the engine's opaque `mem_scope` partition string. **Engine-wired** (#601 S14-4): the loop consults the seam at two scope-keyed sites — the handover **read** site appends an advisory lesson `brief` next to the Memory brief, and the task-completion **write** site reports the feedback as a scoped lesson (gated on a registered provider). The write site is **re-homed onto the `post_feedback` Hook-Bus** (#602 2.3/#804): `gx10._lessons_consumer_hook` (registered on provider presence via `_apply_lessons_consumer`) reports on a fresh completion through the real `_advance_pipeline` wrapper, outside the vault lock — one consistent reflection path (no double-report on an already-done re-advance). Both lazy-imported + fail-soft. **Superseded by the always-on ACE core (#863):** the write path is now ACE's `_ace_consumer_hook` (submits a `Trajectory` to the background ReflectionWorker — `_lessons_consumer_hook` is no longer wired), and the read site injects the query-aware playbook `context_for` when the ACE `PlaybookStore` is the provider (falling back to the string `brief` for a foreign provider). The seam contract (protocol, `set_provider`, fail-soft) is unchanged — ACE just registers a richer provider. |
| **Lesson distiller (provider)** (`engine.lesson_store.EngineLessonStore`) | **wired + tested** | The one concrete `ack.lessons` provider the engine registers (epic #602 S602-5): typed distiller categories (last-failure-reason / best-known-path / known-bad-strategy / user-preference — **provider-internal**; the public seam stays string-only), query (term-overlap) + recency ranking, per-scope compaction, a **scope-keyed persistent backend** (one JSON file per opaque `mem_scope` under `ironclad_home()/lessons`, hashed to a safe name — **never** the global WARM session; imports nothing from `engine.memory`/`engine.warm`), and the optional `forget(scope)` purge. **Superseded by the always-on ACE `PlaybookStore` (#863):** `lessons.enabled` no longer gates a provider — `_apply_ace` registers the `PlaybookStore` unconditionally. `EngineLessonStore` remains as the **legacy backend + the ACE migration source** (its on-disk lessons are replayed into the playbook on first wiring); the class + its typed/ranking/compaction semantics stay tested. C1 = **project-private lessons only** (the global `user_preferences` tier is deferred — needs the curated-global store + a `promote()` redactor). Fail-soft throughout (a corrupt/missing scope file reads as empty). |
| **Failure-classification taxonomy** (`ack.failure_class`) | **wired + tested** | The shared, string-valued `FailureClass` enum naming *why* a step failed (epic #602 S602-3) — the single vocabulary the reflection layer reads instead of re-deriving its own. `classify_emission_failure(message, detail)` is a **pure, deterministic, rule-based** mapper over the exact error the validate→reask loop already records (never a model, never raises; conservative default `SCHEMA_INVALID`); the engine `providers.result_failure_class` re-maps the 3-class run taxonomy onto the same enum (one SSOT). `validated_emit` attaches an **additive, advisory** `ValidatedEmitResult.failure_class` on a terminal failure — `None` on success and on any result built without it (**byte-identical** to the pre-#602 shape); it **never** affects control flow on the fail-closed path. Public ACK surface (advisory labels, **not** the contract-SSOT). The field is **produced** on the validate→reask path AND — opt-in per `strategy.enabled` (#602 2.4/#805) — recorded at the **code-agent failover** (`gx10._record_failure_class` → `gx10._last_failure_class()`, surfaced in the `/feedback` response) for the Strategy Revisor consumer. The **Quality breaker** consumer is now **wired** (#808); the **Strategy Revisor** application follows (#806). |
| **Process-Level Self-Correction** (`ack.process`) | **wired + tested** | Correct the *workflow*, not the response (epic #602 S602-6). A **pure** policy — `distill_process_lesson(ProcessSignal)→ProcessLesson` (success → a reusable working-path; a missing clarification → a gather-up-front note; else None) + `format_process_hint(texts)` — both deterministic + never-raising. OPT-IN engine wiring: at completion the **`post_feedback` Hook-Bus consumer** (`gx10._process_consumer_hook`, registered per `process.enabled` via `_apply_process_consumer`; #602 2.2/#803) stores a TYPED process-lesson via the **concrete** `EngineLessonStore` (`record`/`by_category` — not the string-only seam) on a fresh completion — **re-homed** from the inline advance site onto the bus, outside the vault lock (one consistent reflection path; an already-done re-advance does **not** double-record). The `pre_turn` **read** (`_process_hint`) folds known working-approaches into the prompt prefix (alongside RAG/steer) — a direct prompt-assembly call, since the observer-only bus cannot inject prompt content. **Subsumed by ACE (#863):** the `_process_consumer_hook` synchronous write is retired — ACE's `reflect→curate` owns the reflection path — but the TYPED surface **never silently breaks**: `_concrete_lesson_provider` is now duck-typed (a `record`/`by_category` capability probe), so it resolves to the ACE `PlaybookStore` and `_process_hint`/`record`/`by_category` keep working (gated only on `process.enabled` for the read). Fail-soft, never mutates the fail-closed path. Public ACK surface (`ProcessSignal`/`ProcessLesson`/`ProcessLessonKind`/`distill_process_lesson`/`format_process_hint`). |
| **Quality Circuit Breaker** (`ack.quality`) | **wired + tested** | A **separate**, agent-agnostic per-task output-quality breaker (epic #602 S602-9) — distinct from the per-peer availability breaker (`_CODE_AGENT_BREAKER`; folding quality in would corrupt failover). `QualityBreaker` tracks the trend of the mark-only verifier scores (`ack.verify`) and trips on **sustained degradation** (`min_consecutive` scores below `threshold`; an at/above-threshold score resets the streak). **MARK-ONLY**: a trip is advisory — escalate/surface (pause-autoplan opt-in), **never a hard-abort**. **Fail-open-safe** (never raises; a hiccup leaves it untripped). OPT-IN via `quality.enabled` (default OFF → no breaker → no-op byte-identical); `_apply_config` builds/clears the engine's `_QUALITY_BREAKER`, `_quality_breaker()` exposes it. **Wired (#602 2.7 / #808):** a `post_handover` consumer (`gx10._quality_consumer_hook`, registered per `quality.enabled` via `_apply_quality_consumer`; default OFF → no hook → byte-identical) feeds the mark-only Verifier score (`gx10._last_verdict()`) into the breaker on every staged handover and **surfaces** a sustained-degradation trip (`gx10._quality_tripped()` + a `[quality]` advisory line) — **advisory, never a hard-abort**. Public ACK surface (`QualityBreaker`/`QualitySnapshot`). |
| **Verifier / Evaluation** (`ack.verify`) | **wired + tested** | A **mark-only** behavioral-evaluation layer (epic #602 S602-4): `VerdictResult` (passed/score/reason) from three opt-in, transport-injected, secret-free verifiers — `verify_rules` (deterministic predicates), `verify_grounding` (claims grounded by an injected `retrieve`), and the async `verify_with_judge` (LLM-as-judge over the injected `chat`, **budget-gated** via an injected ledger duck-typed on `dispatch.BudgetLedger` — skips when unaffordable, charges only a completed call). **MARK-ONLY**: a verdict can neither relax nor tighten a gate (the fail-closed core is untouched). Every verifier **never raises**; **default-off byte-identical**. **Wired (#602 2.1 / #802):** the engine runs the Verifier on the **dev-task pipeline** — a `pre_handover` Hook-Bus subscriber (`gx10._verifier_hook`, registered per `verify.enabled` via `_apply_verifier`; default OFF → no hook → byte-identical) evaluates each staged task: behavioral rules over `task_json` + (when a memory tier is up) grounding of the handover's claims against the cold store. It stores a **mark-only** `VerdictResult` (`gx10._last_verdict()`) for the Quality breaker (#602 SUB-9 / 2.7) to read — it **never gates** a handover. The opt-in **LLM-judge** (`verify_with_judge`) stays a separate explicit activation (it charges the budget ledger) and is **not** run by this hook; per-`TaskType` eval selection via `LoopProfile.eval_verifiers` (8b) rides this runner. Public ACK surface (`VerdictResult` + the verifiers). |
| **Loop Profiles** (`ack.loop_profile`) | **wired + tested** | Per-`TaskType` loop budgets (epic #602 S602-8a): the **pure** `resolve_loop_profile(...)` deep-merges code defaults ← `loop_profiles.default` ← `loop_profiles.by_type.<type>` into a `LoopProfile` (max_iterations / retry_budget / effort) — only present keys override, `retry_budget` clamped to the hard re-ask ceiling, **never raises**. Schema + defaults in the engine config tree (`loop_profiles`, **empty by default** → falls back to the globals `MAX_ITERATIONS` + re-ask budget → **byte-identical**, single-sourced); `_loop_profile()` (the **default** profile) drives the chat loop's iteration bound. **Seam status (honest):** the **default** profile is live (the chat loop bound), but `by_type` per-`TaskType` **`retry_budget` is now consumed on the dev-task pipeline** (#602 2.6 / #807): `gx10._failover_budget` resolves it per the staged task's type for the code-agent failover escalation (`gx10._revise_on_failure`), layered over `strategy.budget` and clamped to the hard ceiling (a per-type override can only LOWER it). The chat loop's `max_iterations` still uses the default profile (it has no per-task type); `effort`/`eval_verifiers` ride the Verifier (8b). An operator (or the private `conf/loops` override layer — NOT `core/conf`) tunes the default limits; the public default is unchanged. Public ACK surface (`LoopProfile`/`resolve_loop_profile`). **8b wired** (#809): `LoopProfile.eval_verifiers` (the per-type `eval` key) **selects which mark-only verifiers the per-task Verifier runs** — consumed by `gx10._verifier_hook` (empty → the default rules+grounding set; the async LLM-judge stays a separate opt-in consumer). |
| **Strategy Revisor** (`ack.strategy`) | **wired + tested** | The pure failure→action policy (epic #602 S602-7): `revise(failure_class, attempt, budget) → Strategy` (the SSOT) maps a `FailureClass` to a targeted `StrategyAction` (inject-context / narrow-or-clarify / switch-retrieval / ground-then-answer / complete-output / resolve-policy / repair-schema / fail-over / human-escalation) instead of "retry the same way", escalating to a human when the budget is spent. **Pure / deterministic** (no transport, no model, never raises). Two **opt-in** application seams: `validated_emit`'s optional `strategist` param appends the strategy hint to the re-ask turn (**byte-identical when not passed**; a strategist error is swallowed), and the engine `providers.code_agent_strategy` maps a code-agent run result through the same SSOT. **Wired (#602 2.5 / #806):** the engine consults the Strategy Revisor on the **code-agent failover** — `gx10._revise_on_failure` runs `providers.code_agent_strategy(result, attempt, budget)` per task on the `/feedback` failure path and **surfaces a `HUMAN_ESCALATION`** when the per-task attempt budget (`strategy.budget`) is spent, instead of an endless silent failover (`gx10._last_strategy()`; a successful run resets the counter). OPT-IN per `strategy.enabled` (default OFF → byte-identical); **MARK-ONLY** (it surfaces, never adds a new hard-abort). The richer re-ask actions (inject-context / switch-retrieval) ride `validated_emit`'s `strategist` in the MPR plugin, not the code-agent failover. Public ACK surface (`Strategy`/`StrategyAction`/`revise`). |
| **Loop-Intelligence Hook-Bus** (`ack.hooks`) | **wired + tested** | The keystone seam (#602 2.0 / #690) the reflection consumers subscribe to instead of hard-wiring call-sites into `gx10`: a standalone, dependency-inverted, fail-soft event bus over the agent-loop boundaries. The engine **publishes** 8 canonical events — `pre_turn`/`post_generate`/`post_toolresult` from `run()`, `pre_handover`/`post_handover`/`pre_advance`/`post_feedback` from the `_stage_handover`/`_advance_pipeline` wrappers (outside the `_vault_lock`), and `escalation` when a HUMAN_ESCALATION fires (#1083). `register_hook` is additive + idempotent (fail-loud on an unknown event / non-callable); `dispatch` is **observer-only** (a hook may ABORT, never relax a gate), **fail-soft** (a bad subscriber never breaks a turn), cancel/budget-aware, copy-on-write + snapshot for the multi-threaded engine, and an **O(1) no-op when no hook is registered → byte-identical default**. The reflection consumers now ride the bus: the `pre_handover` Verifier (#802), the `post_handover` Quality feed (#808), and — superseding the #602 `post_feedback` Process-SC (#803) + Lessons (#804) writes — the always-on **ACE `post_feedback` consumer** (#863), which submits a `Trajectory` to the background ReflectionWorker. The whole reflection loop on one wiring path. A **default-off escalation → webhook notifier** (`engine/notify.py`, #1083) rides the `escalation` event: when `notify.webhook` (a deploy secret via `GX10_NOTIFY_WEBHOOK` — never a URL literal in core) is set, a HUMAN_ESCALATION is POSTed as Slack-compatible JSON to a webhook so it reaches an off-duty human instead of only printing to a console; with no URL the consumer is not registered (byte-identical). Covered by `test_hooks.py` + `test_hook_wiring.py` + `test_notify.py` (7). |
| **ACE playbook — data model** (`ack.ace.playbook`) | **wired + tested** | The itemized **Bullet** (stable id + helpful/harmful usage counters + categorical tags) inside a sectioned **Playbook** (the four canonical ACE sections + custom) — the foundation of the Loop-Intelligence → **ACE** (Agentic Context Engineering, arXiv 2510.04618v3) extension, epic #855 cluster ACE-DATA (B-001/B-002/B-003/M-001). Human-readable **and** machine-parsable `render()` (explicit `PLAYBOOK BEGIN/END` boundaries + per-bullet `[id]` + counters + tags, so a Generator can cite which bullets it used) + a **versioned, lossless JSON round-trip** (ids, counters, tags and the id-minting counter all survive a reload; a newer `schema_version` is refused fail-closed). **Pure / stdlib-only** (imports nothing from the engine; ships in the wheel as `ack.ace`). **Wired** as the always-on engine `PlaybookStore` backend (#863 — see *ACE engine wiring* below) and consumed by the Reflector/Curator (#858/#859), grow-and-refine (#860) and Generator (#861). Covered by `test_ace_playbook.py` (16). |
| **ACE Reflector** (`ack.ace.reflector`) | **wired + tested** | The ACE role (epic #855, cluster ACE-REFLECT: E-001/E-002/E-003/E-004 + N-001/N-002) that distils reusable insights from a reasoning **trajectory** + natural execution feedback (**label-free**) and rates which used bullets were helpful/harmful/neutral (E-004): a structured `Trajectory` input + `ReflectorOutput` (candidate bullets + ratings) with optional **iterative refinement** over N rounds (product default 1). The LLM transport is **injected** (`chat`, mirroring `verify_with_judge`) → stdlib-only / clean-room-safe; **fail-soft** (a transport/parse error → empty result, never raises). The Curator (#859) turns its insights into deterministic deltas; the async worker + budget gate + real orchestrator transport are wired by the engine `PlaybookStore` + `_apply_ace` (#863). Covered by `test_ace_reflector.py` (8). |
| **ACE Curator** (`ack.ace.curator`) | **wired + tested** | The ACE role (epic #855, cluster ACE-CURATE: F-001/F-002/F-003 + C-001/C-002 + N-003) that integrates Reflector insights into the playbook as a compact **delta** (`Delta` = `{reasoning, operations}`, F-003) and **merges it deterministically** (the ACE cost/latency win — no monolithic LLM rewrite, no context collapse): `curate` builds add/rate ops from the Reflector output (section-validated F-002, localized-per-bullet C-002, delta-not-rewrite C-001); `apply_delta` merges in place (add / bump helpful-harmful counter + tag / tag / remove). **Deterministic + pure/stdlib-only**; robust (an op on a missing bullet / unknown op / empty content → skipped + counted, never raises); `Delta.from_json` also parses an externally-emitted delta. Covered by `test_ace_curator.py` (8). |
| **ACE grow-and-refine** (`ack.ace.grow`) | **wired + tested** | Keeps the evolving playbook compact + relevant (epic #855, cluster ACE-GROW: D-001/D-002/D-003 + L-003/L-004). `dedupe` merges near-duplicate bullets **within a section** — **semantic** (cosine over an **injected** batched embedder, threshold default 0.9, L-003) with a **lexical token-Jaccard fallback** for sealed/no-memory deployments + on any embedder error (**fail-soft**); `prune` bounds by bullet-count or rendered-size, removing the **least useful** first (lowest helpful−harmful, then most-harmful, then oldest; D-003/L-004); `refine` runs both **proactively or lazily** (only over budget, D-002). Merging folds counters + unions tags into the survivor. **Pure / stdlib-only**; the model-window (32k) coordination is wired via `PlaybookStore.context_for` + the per-scope cap (#863). Covered by `test_ace_grow.py` (9). |
| **ACE Generator integration** (`ack.ace.generator`) | **wired + tested** | Playbook-guided inference + usage tracking (epic #855, cluster ACE-GEN: H-001/H-002 + N-004). The window fix: the playbook store may be large, but `select_relevant` retrieves only the **relevant bullet subset** per query (semantic via the **injected** embedder, lexical token-overlap fallback, **fail-soft**), `prepare_context` renders that subset (`[id]` + counters + tags) into a `GeneratorContext` + tracks the injected ids (H-002), and `to_trajectory` turns the run output + used ids into the Reflector's `Trajectory` (N-004 → N-001, closing the online loop). **Pure / stdlib-only**. Covered by `test_ace_generator.py` (8). |
| **ACE online adaptation** (`ack.ace.online`) | **wired + tested** | The closed loop composed (epic #855, cluster ACE-ADAPT-ONLINE: G-002/O-001/O-002/L-001). `adapt_once` runs Trajectory → reflect → curate → apply_delta → refine over the playbook in place (**label-free** O-001, **cumulative** O-002, `rounds` L-001), **budget-gated** via an injected ledger (`can_afford`/`charge`, like `verify_with_judge` — skips without an LLM call when unaffordable, fail-soft on a flaky ledger) + no-op when nothing is learned. `ReflectionWorker` is the **async seam**: `submit` is an O(1) non-blocking enqueue (hot-path-safe — drops on a full queue, never blocks the turn) and a background daemon drains it off the turn path, so the `post_feedback` hook never runs the model inline (the C0 correctness requirement). **Pure / stdlib-only**; the hook wiring + real transports are delivered by the engine `PlaybookStore` + `_apply_ace` (#863, below). Covered by `test_ace_online.py` (11). |
| **ACE offline adaptation** (`ack.ace.offline`) | **delivered + tested** | The OFFLINE batch counterpart to the online loop (epic #855, cluster ACE-ADAPT-OFFLINE: A-003/C-003/G-001/G-003/G-004) — an **operator-invoked** build over a dataset, **off any turn path** (so no runtime wiring; the M3 eval harness #865 + an operator offline-build consume it). `build_offline` runs **multi-epoch** (G-003, default 5) over **batches** (A-003, base size 1): each batch's per-sample reflect→curate deltas are computed **independently** (an injected `map_fn` may parallelize) and **merged deterministically** (`merge_deltas`, C-003 — input order, last-wins ADD collision, RATE/TAG/REMOVE accumulated), then applied + refined; budget-gated, **label-free** (labels only for eval), fail-soft per sample. `evaluate` is the test-split **pass@1** (G-001 — uses only the optimized playbook, not the training data); `warmup` is the label-free execution-ledger replay (G-004) seeding the playbook the online loop continues on. **Pure / stdlib-only**. Covered by `test_ace_offline.py` (9). |
| **ACE evaluation** (`ack.ace.evaluation`) | **delivered + tested** | The SHIPPED eval surface + the comparative-baseline cost/latency harness (epic #855, cluster ACE-EVAL: I-001/I-002/I-003 + J-001/J-002/J-003 + L-002). Metrics: `accuracy` (I-003, exact predicted==ground-truth %), `goal_completion` (I-001 Task GC / I-002 Scenario GC — fraction successful + per-difficulty split). Harness: three **BUILT**, `RolloutMeter`-instrumented adaptation strategies (real ACE `reflect→curate→merge`, a monolithic full-rewrite baseline, an evolutionary validation-loop baseline) whose cost is **measured** — `compare_adaptation` proves ACE does **ZERO full-rewrites + ZERO LLM merges** (J-001 — local deltas + deterministic merge) and cuts rollouts **>50%** vs the evolutionary baseline (J-002). `kv_cache_metrics` measures the stable-prefix cacheable ratio of successive renders (J-003 — append-only ⇒ high reuse, a rewrite ⇒ low); `validate_epochs` guards the epochs hyperparameter (L-002 — ≥1, default 5); `EvalReport` aggregates the surface. **Operator-invoked** (off any turn path; no runtime wiring). **Pure / stdlib-only**, fail-soft metrics. Covered by `test_ace_evaluation.py` (13). |
| **ACE robustness + safety** (`ack.ace.robust`) | **delivered + tested** | The robustness/safety layer over the playbook (epic #855, cluster ACE-ROBUST: K-001/K-002/K-003 + Q-001 + M-002). **Graceful degradation:** `quarantine_noisy` drops harmful-dominant (net-negative) bullets so moderate noisy/adversarial updates are rejected without collapse (K-002), and `adaptation_gain` shows a weak reflector still nets **positive after quarantine** while a stronger one gains more (K-001 — monotone in reflector strength). **Contradictions (K-003):** `detect_contradictions` finds opposite-polarity, same-section bullets (token-overlap + negation heuristic); `resolve_contradictions` keeps the higher-utility belief. **Selective unlearning (Q-001):** `unlearn` removes individual bullets **by id** — no retraining, no scope-wide wipe. **Versioning (M-002):** `version_id` (deterministic content hash) + `diff_versions` (traceable added/removed) + `PlaybookHistory` (snapshot / **rollback** to a known-good version) — the reversible safety net the item-level unlearn complements. **Pure / stdlib-only**, never raises. **Operator-reachable (#1082):** the `PlaybookStore` persists a bounded per-scope version history (next to the scope playbook) and exposes `snapshot` / `versions` / `rollback [<version>]` / `unlearn <id…>` (each mutating verb snapshots first, so it is itself reversible), wired to the `/ace` verbs — so an operator can undo a regretted adaptation or forget a bad bullet live. **Learned-state safety (#1070):** with `ace.safe_promote` on, `PlaybookStore.adapt` snapshots the pre-adapt playbook before every online adaptation (an operator/auto rollback point) and, when an eval scorer is wired (`set_transports(eval_fn=…)`, deploy-provided), **auto-reverts a measured regression** (`regression_verdict` — never persists a delta that lowered the score; fail-open on a broken measurement); default-off, byte-identical. Covered by `test_ace_robust.py` (10) + `test_ace_playbook_safety.py` (6) + `test_ace_safe_promote.py` (5). |
| **ACE engine wiring — the always-on loop-intelligence core** (`engine.playbook_store.PlaybookStore` + `gx10._apply_ace`) | **wired + tested** | The keystone that makes ACE the engine's loop-intelligence mechanic (epic #855 cluster ACE-WIRE / #863) — **ALWAYS ON, no enable flag**, SUPERSEDING the #602 string-lesson + Process-SC `post_feedback` consumers. `PlaybookStore` wraps one `ack.ace.Playbook` per opaque `mem_scope` (one schema-versioned JSON file under `ironclad_home()/ace_playbooks`) and is a drop-in for the lesson seam in BOTH senses: the string `ack.lessons.LessonProvider` surface (`get_lessons`/`report_lesson`/`brief`) the engine read/write sites use, AND the typed `record`/`by_category`/`forget` surface the in-process Process-SC consumers couple to — plus the ACE-native query-aware `context_for` (the 32k-safe Generator handover read — injects only the relevant bullet subset) and the budget-gated online `adapt`. `_apply_ace` (in `_apply_config`) registers it as the provider, wires the `post_feedback` `_ace_consumer_hook` that **submits** a `Trajectory` to a background `ReflectionWorker` (reflect→curate→refine runs OFF the hot path — never inline), and starts the worker; the orchestrator-model `chat`, the memory-service **`/embed`** adapter (BGE-M3, 1024-d) and the token budget are injected. `_concrete_lesson_provider` is now **duck-typed** (a `record`/`by_category` capability probe, not `isinstance`) so **Process-SC never silently breaks**. The legacy #602 `EngineLessonStore` tree is **migrated** into the playbook on first wiring. A foreign extension provider is never clobbered (**richer-wins** — ACE steps back while one is registered). **Fail-soft throughout**: with no orchestrator model reachable ACE no-ops and the playbook stays empty. **Live feedback (M4-0 / #877):** the handover injection site records which bullets it injected (keyed by task_id) and the `post_feedback` consumer threads those **real `used_bullet_ids`** + a **label-free outcome** (`OK: pipeline advanced`⇒`success`, `ERROR: pipeline step failed`⇒`failed`) — so the live loop actually learns from successes AND failures + rates the used bullets (E-004/H-002/E-001/O-002); already-done/trivial-error advances are skipped. Covered by `test_ace_playbook_store.py` (15) + `test_ace_wire.py` (7) + `test_ace_process_supersede.py` (4) + `test_ace_livefeedback.py` (8). |
| **ACE dev-process trajectory** (`ack.ace.devtraj`) | **delivered + tested** | The pure ledger→`Trajectory` adapter for the dev-process self-learning loop (epic #855 cluster ACE-DEVTRAJ / #878, DP-1) — Variant A (ledger-derived). `ledger_to_trajectories` re-projects already-parsed dev-loop ledger payloads (the same boundary-clean `.devloop/ledger.jsonl` the engine `lifecycle_projector` reads — consumed as plain data, NO engine import) into one ACE `Trajectory` per unit: driver transitions grouped by `unit` (abort by `abort`); ordered, timestamp-free leg summaries (`GATE gate passed` / `REVIEW review-evidence passed` / `…FAILED: <reason>` / inert-review marked); a **label-free** outcome ∈ {`reached-human-merge-gate` (the driver stops at the human MERGE gate — not a confirmed merge), `aborted`, `blocked`, `in-progress`}; `used_bullet_ids=[]` (M4-3 correlates the per-unit injected bullets). **Pure / stdlib-only**, schema-drift tolerant, never raises; a DELIVER record (no `unit`) is skipped. The read-half consumed by the M4-2 engine learn-trigger. Covered by `test_ace_devtraj.py` (9). |
| **ACE dev-process learn-trigger** (`gx10._ace_scan_dev_ledger`) | **wired + tested** | The engine half of the dev-process self-learning loop (epic #855 cluster ACE-DEVWIRE / #879, DP-1/DP-4). OFF the hot path it scans the dev-loop ledger (read as plain data via `_read_ledger_payloads` — Variant A, no driver change / no new public seam), projects it via `ack.ace.devtraj` into per-unit Trajectories, and submits each **newly-terminal** unit (reached-human-merge-gate / aborted) to the existing `ReflectionWorker` **exactly-once** (a persisted submitted-units set under `ironclad_home()` survives restarts). A chain-tampered/unreadable ledger is **skipped fail-closed**; no worker/bound-scope ⇒ no-op; `used_bullet_ids` empty until M4-3. Wired into the `/lifecycle gate` command (the dev-loop's DELIVER-leg ledger touchpoint, reusing the payloads it read — advisory, does not affect the gate). Distinct per-UNIT signal from the in-process per-handover `post_feedback` hook (run.py is a separate process → no double-learning). Covered by `test_ace_devwire.py` (7). |
| **ACE dev-process used-bullet correlation** (`gx10._ace_unit_keys`/`_ace_persist_injected` + `playbook_store.record_unit_bullets`) | **wired + tested** | Closes the used-bullet loop for the dev-process (epic #855 cluster ACE-DEVBULLET / #880, DP-2). The handover injection site (#863) DURABLY records which bullets it injected, keyed by the engine task id **and** the issue#s the handover references (the standard `Closes #N` linkage); the M4-2 ledger scan reads it back by the unit (issue#) → `Trajectory.used_bullet_ids`, so the Reflector rates which dev-loop bullets helped (E-004). **DP-2 (playbook reaches the coder) is satisfied for the handover+feedback coder-addressing mode** — the autopilot / GitHub-only / file-based-with-handovers / public `ack.devprocess.stage_handover`→engine-driver flows, where the coder reads the handover md `context_for` injected (the `agent_id` IS the handover/feedback filename token). The machine-gated dev-loop worktree-agent injection (no handover seam, file-based) is delivered in **#894** (`scripts/devloop/ace_inject`), which also records the per-unit correlation for the worktree path. `[]` when no correlation (weaker, not wrong); fail-soft. Covered by `test_ace_devbullet.py` (6) + the cache (2 in `test_ace_playbook_store.py`). |
| **▾ Dev-Process Self-Learning (M4, epic #855)** — ACE learns from the dev-process itself (Variant A, ledger-derived) | **wired + tested** | **M4 complete (#877 M4-0, #878 M4-1, #879 M4-2, #880 M4-3, #881 M4-4).** The dev-loop's own transition ledger (`.devloop/ledger.jsonl`) — written by BOTH the public generic `ack.devprocess` driver AND the internal DEV-3 (`scripts/devloop`) in the same schema — is projected (`ack.ace.devtraj`) into per-unit Trajectories, scanned off the hot path into the `ReflectionWorker` (exactly-once), correlated to the bullets each unit's handover injected (#863), and learned into the playbook. **Proven END-TO-END for both dev-processes** through one core adapter (DP-3), fail-soft on a degraded/tampered ledger (DP-4), exactly-once vs the per-handover hook. The injection reaches the coder via the **handover+feedback** mode (the `agent_id` = the handover/feedback filename token; `claude --print` reads the `context_for`-injected handover) AND — via **#894** — the machine-gated dev-loop's WORKTREE coder (which has no handover seam): `scripts/devloop/ace_inject` writes the unit's playbook into the worktree's `CLAUDE.local.md` (git-excluded, so never in the unit diff) before the agent runs + records the per-unit correlation for the M4-2 scan. Both dev-process coder modes now get the playbook. e2e: `test_ace_devproof.py` (6) + `scripts/devloop/tests/test_devloop_ace_inject.py` (3). |
| **▾ MPR-for-Architecture (M5, epic #855)** — a gated, well-founded architecture proposal at a decision fork (`ack.ace.fork` + MPR) | **wired + tested (M5 complete)** | The *propose* layer: at an architecture fork, MPR produces a multi-perspective decision-matrix as a proposal, and ACE records the decision + outcome so the next comparable fork is pre-informed. **M5-1 ACE-FORKSIG (#882):** the pure, boundary-clean data contract — `ForkSignal` + `ForkResolution` + a drift-tolerant ledger adapter on the `FORK`/`FORK_RESOLVED` surfaces; Fork-detection = **Variant A (declared fork)**, reversible; pure/stdlib-only. **Variant B — ambiguity auto-detect (#1066):** `detect_ambiguity` is a pure, precision-first pre-flight scan that flags requirement underspecification (uncertainty markers · an open question inside the requirement · either/or · vague qualifiers · hedges) and emits the same halt-to-ask `ForkSignal`; wired **default-off** (`safety.ambiguity_detect` / `GX10_AMBIGUITY_DETECT`) into the `pre_handover` Hook-Bus so an autonomous agent that did NOT notice the ambiguity is warned with the fork question + options (observer-only; full pipeline-HALT enforcement is operator policy) instead of guessing — the safety net the no-guessing prompt convention lacked (`test_fork_ambiguity.py` 7). **M5-2 ACE-FORKMPR (#883):** the gated, off-hot-path MPR invocation — a new gate **`ace.fork_mpr.enabled` (default OFF)** fires MPR's `architecture-decision` panel (`run_tool('mpr_research', domain_hint='architecture-decision')`) on a dedicated `_ACE_FORK_WORKER` when a `ForkSignal` is recognized at the `/lifecycle gate` touchpoint; **pre-informed** by prior fork bullets (`context_for`, MPR-A-5); exactly-once; **gate OFF ⇒ byte-identical**; fail-soft. **M5-3 ACE-FORKPROPOSE (#884):** the produced matrix is recorded as a fork-proposal pointer bound to the unit (`playbook_store.record_fork_proposal`) and `_ace_fork_proposal_for(unit)` renders it as a **recommendation only** (top-ranked option + ranked matrix + dissent + "you decide; ACE learns") — a generic, boundary-clean seam both dev-processes attach to their operator ask (MPR-A-3), surfaced by the read-only **`/fork [unit]`** command (C2 #903 wired the output leg to a production caller); no matrix ⇒ the ask is unchanged. **M5-4 ACE-FORKLEARN (#885):** a resolved fork (`ForkResolution`) becomes a fork-decision `Trajectory` (query = the fork question — the comparability key; outcome = chose <option> → <outcome>; `used_bullet_ids` = the prior fork bullets that seeded the query) submitted to the existing `_ACE_WORKER` → reflect→curate writes a bullet in `strategies_and_hard_rules`, so `context_for` pre-informs the next comparable fork — making M5 a *loop* (propose→decide→record→pre-inform); gate OFF ⇒ no learning; exactly-once (distinct from M4-2/#863). **M5-5 ACE-FORKPROOF (#886):** the boundary-clean e2e capstone — the whole loop (fork→panel→proposal→resolution→learned→next fork pre-informed) proven for BOTH dev-processes through the one seam, plus the gate-off no-op (byte-identical) and the full fail-soft matrix. **M5 ships as one self-consistent, gated (default OFF), fail-soft capability on top of always-on ACE.** Covered by `test_ace_fork.py` (8) + `test_ace_forkmpr.py` (6) + `test_ace_forkpropose.py` (7, incl. `/fork`) + `test_ace_forklearn.py` (6) + `test_ace_forkproof.py` (4). |
| **Command-spec foundation** (`engine.command_spec`, epic #927) | **wired + tested (foundation)** | A hand-authored, machine-readable **parallel description** of every slash-command verb — flags, choices, and a per-command danger-tier (`read_only`/`mutating`/`destructive`/`costly`) plus the six boot-only config keys. It does **not** drive dispatch — `engine.gx10._dispatch` stays the untouched fail-closed executor — it is the single machine-readable source describing the command surface. A **non-vacuous spec↔dispatch parity guard** (`scripts/ci/check_command_spec_parity.py`, #940) derives the verb set from `_dispatch` source (AST over the branch literals, excluding the dynamic prompt-name + `else` fall-through), imports the real `_FROZEN_CONFIG_KEYS`, and introspects `ack.generator.build_parser` — failing the build if the spec drifts from any of the three. It is served via `GET /catalogue` (#931) so the terminal client **generates its server-command completions from it** (verbs the static client list missed — lifecycle/fork/ace — now surface; the static list is the cold-start fallback for a `token`/`sealed` deployment). Spec-derived **discovery** (#932): `/config keys` lists the settable dotted keys (boot-only flagged) and `/config set` now **refuses an unknown-root key** (a typo) instead of silently writing it with a false success; `/skills` surfaces each tool's parameters; `/help` appends the commands grouped by danger-tier. A `--tree` resolver (`_git_head_tree`, #933) defaults `/lifecycle gate` to the committed git HEAD tree when `--tree` is omitted — fail-soft to `""` (the `BLOCKED: no delivery tree_sha` path still fires), with an explicit `--tree` always overriding (the one deliberate, scoped read-only git call in core). Both clients' `classify()` resolve **aliases / unambiguous-non-destructive-prefix / did-you-mean** (`resolve_command`, #934) — deterministic + zero-cost (no model): a short alias or safe prefix is corrected to the canonical command (re-parsed through the untouched `_dispatch`), and a mistyped `/verb` becomes a `suggest` shown by the client (never forwarded, so it costs no model turn), while a `/<prompt-name>` still forwards to the server's prompt resolver. A **server-side confirm-before-execute contract** (`engine.server._confirm_required`, #935) gates destructive ops uniformly: `/chat` + `/chat/stream` reply `{needs_confirm}` for `/project delete` (danger-tier from the spec, never model-graded) instead of executing; every client shows the warning and re-runs with a trailing `--yes` (stripped → `confirm: true`). Fail-soft; read-only/mutating/costly are unaffected (operator-scoped to destructive). Spec-derived **guidance** (`command_spec.guided_usage`, #936) renders a single-source usage line (subcommands + flags; required bare, optional `[bracketed]`, choices `{a|b}`) wired into `/lifecycle`'s usage, and the `/catalogue` command entries carry structured `subcommands` + `flags` so a client builds argument autocomplete/guided-input from the one source; `/ace` defaults its `--ledger` (like `/lifecycle`) and reports a plain-language `eval` verdict (J-001/J-002 kept only as a parenthetical). The terminal client renders this: **argument autocomplete** (`clients/ink` `argCompletions`, #937) completes a command's subcommands / flag names / flag choices from those structured `/catalogue` entries once past the verb (accepting inserts the token into the line), alongside the server-driven did-you-mean (#934) and confirm (#935) surfaces. The new engine-chrome outputs (the confirm reason + `/ace` verdict) are localized via `_msg`/`messages.py` (EN source + DE overlay, no hardcoded German), and the whole surface is documented in [`command-ergonomics.md`](command-ergonomics.md) (#938). Two fail-closed drift guards keep the mirror honest (#939): `check_ink_command_parity.py` (the ink `ALIASES`/`UNSAFE` == the command-spec, and the ink server-verb subset **covers** `command_spec.verbs()` so the did-you-mean net is never blind to a spec verb — #952) and `check_ink_test_count.mjs` (the documented TS counts == the `node:test` suite). Spec-derived `guided_usage` is wired into every flag-heavy worst-offender (`/config set`, `/ace`, `/project`, `/generate`) so their usage is single-source, not hand-written (#953). The remaining engine chrome (`/config keys` header/values+types, `/help` tiers, unknown-root refusal, `/skills` params) is localized via `_msg`, and the destructive-confirm message is a single localized line (#956). Every client renders it (#955): ink field-by-field + the 3 Python REPLs via a shared `render_guide`, detecting `needs_guide` on `/chat/stream` like `needs_confirm`. A server-side structured **needs_guidance** contract (`engine.server._guide_required`, #954) answers an explicit `/<verb> ?` / `--guide` with the spec-derived fields (name/required/choices/default/type) + usage + canonical echo, uniform across clients (like the confirm gate), `_dispatch` untouched. Both the confirm and guide gates fire on the **slash-stripped client wire form** (what `classify()` sends + `_dispatch` consumes), tolerating an optional leading `/` (#962 — they had required the `/` that clients strip, which left both contracts dead end-to-end; a classify→gate regression test guards it). Covered by `test_command_spec.py` (11) + `test_command_spec_parity.py` (4) + `test_catalogue_endpoint.py` + `test_config_runtime.py` + `test_lifecycle_projector.py` + `test_commands.py` + `test_server_split.py` + `test_ace_warmup.py` + `test_ink_command_parity.py` (3) + `clients/ink` `classify.test.ts`/`menuModel.test.ts`. |
| **Self-maintaining vault** (`reconcile_vault`, `/initiative reconcile`) | **wired + tested** | Deterministic, **LLM-free**: regenerates `INDEX.md` (grouped, Obsidian `[[links]]`, manual prose preserved) and injects an idempotent "Verwandt (auto)" relation block into curated docs (shared tags / title reference). Also generates a **typed-edge graph** (#601 S12c): typed frontmatter edges (`depends_on`/`refines`/`supersedes`/`relates_to`/`implements`/`blocks`) → a machine-SSOT **`GRAPH.json`** (full-relpath node keys, no timestamp ⇒ idempotent; unresolvable targets flagged `dangling`) + a human **`LIFECYCLE.md`** view, both next to `INDEX.md` (excluded from the doc scan; HTML markers frozen). Also models a **composable lifecycle** (#601 S12d): an ordered stage set `idea→design→adr→spec→tests→proposals→reviews→delivery`; a doc's `stage` frontmatter composes into `lifecycle_state` (present/current/gaps/complete), surfaced in `GRAPH.json` + `LIFECYCLE.md`, with a fail-closed `can_advance_stage` transition guard (the reusable primitive the DELIVER completeness gate consumes). Auto-fires index+graph after writes; full link pass on the explicit command. **Project-scoped + cross-track** (#601 S12e): `reconcile_active_project` reconciles every initiative in the current track; `reconcile_all_tracks` is the scheduled cross-track reconciler (sweeps `main` + every `.tracks/<track>/`, fail-closed per track, idempotent), surfaced as `/initiative reconcile all`. **i18n** (#601 S12e-2): the vault/initiative prose routes through `engine/messages.py` — English source/default (public-grade export), German overlay by `gx10.LANGUAGE`; the `ironclad:*:auto` markers stay frozen. **Evidence projector** (#601 S13a / AD-7): `project_evidence(stage, …, tree_sha, content_hash)` appends an idempotent, append-only `type: evidence` stage-tagged doc under `<slug>/evidence/`; `lifecycle_completeness(slug, required_stages, tree_sha)` is the fail-closed DELIVER-leg gate (every required stage present + bound to the delivery tree_sha). **The DELIVER-leg wiring is now LIVE (#601 S13b / #632):** a `/lifecycle gate` engine command reads the dev-process ledger (`<repo>/.devloop/ledger.jsonl`, as plain data — boundary-clean) and, via the pure `engine.lifecycle_projector` (primitives injected), projects its transitions into stage-tagged evidence — GATE-green→`tests`, REVIEW-pass→`reviews`, DELIVER→`delivery` — bound to the delivery `tree_sha`, then runs `lifecycle_completeness` and reports `READY`/`BLOCKED` fail-closed (missing slug/tree_sha, tampered/unreadable ledger, or a missing required stage all BLOCK). The shipped default verifies the `delivery` stage (the one the producer logs today); `tests`/`reviews` are opt-in via `--stages` and become enforceable once the dev-loop driver's `log` seam feeds the per-unit transitions to the ledger (#830). |
| ACK contract gate at `stage_handover` | **wired + tested** | Soft path (`_ack_validate`): task_json validated against `TaskSpec`, fail-closed → reask. |
| Function-calling robustness (every tool boundary) | **wired + tested** | Validate→reask on **all** tool arguments, not just task_json: malformed JSON or a schema violation (required + types) is returned to the model as the tool result so it re-emits — never silently degraded to empty args. The malformed call's `arguments` are **sanitised to valid JSON** (`_valid_tool_args_json`) — both at the append site AND over the whole history at the single send choke-point (`_sanitize_tool_call_history` in `_make_completion`, so a malformed call reloaded from a persisted `session.json` can't 400 the first request after a restart): vLLM `json.loads()` the stored tool-call arguments when rendering the request, so an un-parseable string (a small model's oversized `write_file` content) would otherwise hard-400 the turn and defeat the loop — now the request always renders and the model gets its retry. For LARGE content the model authors **escape-free**: `write_last_reply(path)` persists the model's previous reply text (produced as ordinary output, no JSON escaping) and `write_file(mode='append')` builds a file in chunks — a warn-only integrity guard flags a write whose emitting generation was cut off by the token limit (`finish_reason=length`), #1048. `edit_file(path, old_string, new_string, replace_all?)` does a targeted exact-string replace (unique-or-`replace_all`, atomic) so a small change doesn't rewrite the whole file (#1075). Live-verified end to end (native tool turn against the reference model). |
| Model-agnostic tool-call recovery | **wired + tested** | When an OpenAI-compatible endpoint returns no native `tool_calls`, calls the model emitted as **text** (`<tool_call>` tags, fenced json, or a bare object) are recovered — gated to known tool names so a legitimate JSON answer is never hijacked. Removes the hard dependency on a server-side tool parser. |
| Orchestrator Docker image / compose | **wired + tested** | Runs as `ironclad-orchestrator` next to the model. |
| **Memory — multi-tier** (Mem0 cold + warm tier + per-turn context pipeline) | **wired + tested** | Cold: `engine/memory.py` → Mem0-style service (`GX10_MEMORY_URL`), **model-host-pinned** (GPU/LLM-coupled). Warm: `engine/warm.py` → a Valkey/BSD store (`GX10_WARM_URL`, fail-soft) that **follows the orchestrator** (loopback ideal, a LAN hop to the model host acceptable — #385/Decision D-Valkey). `/health` reports **Cold and Warm separately** (`memory` + `warm` = up/down/off), so a silent Warm outage can't hide behind a Cold-only `memory: up` (#385); the Ink footer shows both. Plus rolling summarization, per-turn RAG (with warm cache-aside), token-accurate budgeting, chunking + recency, the `deep_query_memory` (graph) tool, a model-callable **`remember`** deliberate-write tool (#1076 — persist a durable fact, scope-aware + fail-soft, retrieved later via `query_memory`/RAG), and worker memory (shared summary on read + single-writer reduce). A staged handover gets a **token-budgeted Memory brief** (`MemoryManager.brief`, #458 / FORK-G D1): the shared warm rolling summary + **body-keyed** vector hits + optional relational hits, deduped and trimmed to `context.memory_brief_tokens` (default 1200), fail-soft — so an external coding CLI starts a task with the orchestrator's memory. Default-on where a memory/warm service is configured; store starts **empty** — see below. **Backup/restore** (`scripts/backup.sh` / `scripts/restore.sh` + [`backup-restore.md`](backup-restore.md), #1062): dumps the three tier volumes (Qdrant/Neo4j/Valkey) + the `ironclad-workdir` (vault artifacts · ACE playbooks · audit ledger) to a timestamped archive with retention (keep newest N, unit-tested); restore is `--yes`-gated; scheduled by the operate-phase scheduler (`scripts/scheduler.py`, #1064 — a jobs-config + last-run-state + `--run-due` pass driven by one host cron entry that fans out to backup/prune/scan jobs; see [`scheduler.md`](scheduler.md)). A **read-only Memory MCP** (`memory_mcp.py`, #480 / FORK-G D2) additionally lets an MCP-capable CLI (Codex/Claude) LIVE-query the project memory (`memory_search`/`memory_deep_query`, no write) during a handover — a dependency-free stdio JSON-RPC server the CLI spawns, **gated on the `sealed` trust profile**, **secret-free** (the connection travels in the spawned env, not the wire), scoped to the **project namespace**. Cold + warm are partitioned by the active project/track `mem_scope` (`agent_id` / the `session:`+`ret:` key namespace; #601 S3b/S14-1); cold writes also self-describe their origin `scope` in metadata (#601 S14-5, byte-identical for the base partition). A **scope-aware forget** drops one partition across all tiers (#601 S14-5 / D5): `MemoryManager.forget(scope)` via the Mem0 `/delete_all` route (by `agent_id`), `WarmTier.forget_scope(scope)` over the **exact**-scope session + cache keys (no cascade into deeper tracks), and the optional `ack.lessons.forget(scope)` — orchestrated by `gx10._forget_scope`, **fail-closed on an empty scope**, fail-soft per layer. The memory service **requires `agent_id`** (the `mem_ns` partition) on every write/search and refuses `run_id` as an isolation key (#601 S15); `GET /scopes` lists the partitions present, feeding a **registry-keyed orphan GC** (`gx10._reconcile_orphan_memory`, dry-run by default) that forgets minted partitions with no registered project. |
| **Autoplan** (`/autoplan on\|off [N]`) | **wired (config-gated)** | Ported into the server's queue consumer (`_autoplan_tick`), **decoupled from autopilot** so it works in the split: server plans → client executes → server advances → server plans again. Fires only when `/autoplan on` is set **and** `paths.active_capability_backlog` is configured (no backlog → it disables itself) **and** the channel is not sealed (under `sealed`, with no live session, autoplan pauses: `[AUTOPLAN] paused — channel sealed`). Logic unit-tested. |
| Autopilot auto-launch on the server | **placeholder / by design off** | The server never launches code-agents — its queue consumer drops the internal `_LAUNCH_CMD`; launching is the client's job (the pool). `autopilot.enabled` is itself a runtime-settable toggle, but it has **no launch effect on the server** (it only makes the reconciler enqueue launches the server then discards). When the autopilot *does* run (the local/desktop path), its per-agent logs go to `state_root()/logs` (`.ironclad/logs`), not the project root. |
| Remote turn cancel (Esc / Ctrl+C) | **wired + tested** | `POST /cancel` sets the engine cancel event; the running turn aborts at its next iteration. In the recommended TypeScript client **Esc or Ctrl+C** cancels — it aborts the stream locally (instant return to idle) and POSTs `/cancel`; the legacy Python TUI fires it via Ctrl+C non-blocking. |
| Constrained-emission **hard floor** (grammar) | **available — soft path active by design** | Grammar-constrained decoding (guided JSON) is **verified stable** on the reference GPU (no engine crash, schema-valid output). The ACK exposes it (`constrained_emission` / `emit_validated`) for callers wanting token-level guarantees; the **orchestration engine deliberately keeps the soft validate→reask gate** — it's backend-agnostic (any OpenAI endpoint) and already ~100% reliable, so per-emission grammar buys little. Not a TODO — a decision. |
| **Lodestar** capability→backlog plugin | **opt-in (off)** | `lodestar.enabled=false` by default; demo in `examples/demo-vessel/`. |
| **Security / trust model (Phase d)** | **wired + tested (single-tenant)** | Selectable trust profiles (`security.profile`): `open` (default — no auth, LAN bind, mount allowed), `token` (deployment secret over the LAN), `sealed` (loopback bind behind a client-managed tunnel + secret + session heartbeat; client-facing endpoints refuse and autoplan pauses when no session is live). The token is a **deployment secret, not a user login**. Fail-closed: an auth profile refuses to boot without a secret. Unit-tested end-to-end (no vLLM) **and live-verified** over a real SSH tunnel on the reference GPU (loopback bind, gated routes 401, session open→live→close, a real model turn through the sealed channel — see below). **No multi-user identity/authorization** — single principal only; see the [roadmap](roadmap.md). **The CLI-runner lane (`web_search` / the future `read_offload`) spawns its coder subprocess with a secret-scrubbed, credential-redirected env** (`engine/agent_env.py`, #1052): every secret NAME is stripped from the child env and the git/gh credential-discovery paths are redirected to an empty scratch store, so a prompt-injection in untrusted ingested content can't exfiltrate an inherited token or push via the ambient credential — while `HOME`/`~/.claude` (the coder's own OAuth) is preserved and `CLAUDE_CONFIG_DIR` is never set (#994/#996); fail-closed to a plain scrub if the redirect can't be written. Config/env: `security.profile`/`GX10_PROFILE` (boot-only/frozen), the secret in `GX10_SERVER_TOKEN` (env named by `security.token_env`), `security.session_heartbeat_s`/`GX10_SESSION_HEARTBEAT`, and `security.code_locality`/`GX10_CODE_LOCALITY` (`mount`\|`local` — honored under open/token, forced `local` under sealed, advertised on `/health`). The same gate applies in **`token`** (401 without the Bearer secret, no session needed) and **`sealed`** (also needs a live session). Operator how-to: [`security.md`](security.md). |
| **Core built-in loader** (always-on) | **wired + tested** | Built-in skills/prompts load at startup from a **fixed core dir (`skills/`)**, scanned **unconditionally** — independent of `GX10_PLUGINS_DIR` (`_load_skills`). Built-ins work out of the box, no config. Covered by `test_builtin_loader.py` (4). ADR-0002 #114. |
| **Open plugin surface** (`GX10_PLUGINS_DIR`) — 3rd-party | **wired + tested** | The open, versioned extension contract for **3rd-party/user** skills: drop a Python file with a module-level `CASE` dict + `run(...)` (or a `SKILL.md` playbook) into a `skills/` directory, point `GX10_PLUGINS_DIR` at it, and the engine **discovers it additively — with no core change** (built-ins are no longer routed through it). The CI boundary check keeps `core/` standalone + secret-free; it does **not** sandbox plugins — a plugin runs in-process. See [`plugin-api.md`](plugin-api.md). |
| **Extension SDK** (`ack.sdk`) — separate-repo authoring | **wired + tested** | The **curated, versioned import surface** a plugin builds against from its **own repo** (`pip install ironclad-ai`): `ack.sdk` re-exports the tool/playbook/prompt kinds, the registration/eval `gate`, `derive_tool_schema`, `Localizer`, and the `catalogue` — `ack.sdk.__all__` **is** the public API (everything else under `ack.*`/`engine.*` is internal). Provisional while `0.0.x`, semver from 1.0. A shipped **example plugin** ([`examples/example-plugin/`](../examples/example-plugin/)) shows the separate-repo shape, and the **clean-room builds it against the freshly-installed wheel** (proving a separate repo can build against the published artifact). Covered by `test_sdk.py` (7) + `test_example_plugin.py` (3). [ADR-0004](adr/0004-extension-sdk.md), [`plugin-api.md`](plugin-api.md). |
| **Packaged-plugin loading** (`ironclad.plugins` entry-point) | **wired + tested** | A *packaged* plugin (pip-installed into the deployment) is discovered at startup via the `ironclad.plugins` **entry-point group** — additively alongside built-ins + `GX10_PLUGINS_DIR`, with **no path config and no core change**. Dependency-inverted: the engine resolves each entry point to a plugins dir (package / callable / path) and scans it; it **never imports a concrete plugin** (only the generic group string couples them). Broken entry points are fail-soft. An **export-leak guard** (boundary + export forbidden-literals + `test_export_leak_guard.py`, 4) keeps any **internal** plugin out of `core/` and the public export — the coupling is only the generic group string. Covered by `test_entrypoint_loader.py` (10). [ADR-0004](adr/0004-extension-sdk.md). |
| **Playbook skill kind** (`SKILL.md` + `use_skill`) | **wired + tested** | The second skill kind (ADR-0001): `ack.playbook` parses+validates `SKILL.md` packages and `Registry.discover_playbooks` finds them alongside the typed-tool `discover_skills`; the engine exposes them via the `use_skill` tool with **progressive disclosure** (list metadata → load body → load a reference on demand). Covered by `test_playbook.py` (15). See [`skill-packaging.md`](skill-packaging.md). |
| **Skill generator** (`ack.skillgen`) | **wired + tested** | Spec → a schema-valid scaffold for **both** kinds (typed `CASE`+`run` `.py` with a derived tool schema + an auto-test stub; or a `SKILL.md` playbook package + `references/` + `scripts/check`). Contract-correct by construction; the body is a marked stub for an author/LLM to fill (ADR-0001 D3). The richer paved-road domain scaffold remains `ack.generator` (template-tree render + re-runnable 3-way merge), now with a **built-in collision guard** (#601 S10): a generated per-project item whose capability would shadow a core built-in is refused fail-closed before anything is written. The engine `/generate` command renders the template tree into the **active project's library** (the ctx-resolved `vault/library`, never `core/skills`) with the core built-in set injected as the guard. A `--kind {case,prompt}` selector (#601 S10c) picks the template tree: `case` (default) is byte-identical; `prompt` renders the `new-prompt` tree into a gate-valid `kind: prompt` item (`SKILL.md` + `locales/<lang>.json`) and widens the guard to also cover built-in prompt capabilities (`test_generator.py` 14 + `test_generate_command.py` 13). Covered by `test_skillgen.py` (7). |
| **Skill library catalogue** (`ack.catalogue`) | **wired + tested** | Self-hosted manifest index over **both** kinds (reads the skill's own metadata — no separate registry file to drift): `capability`/`kind`/`version`/`type`/`domain`/`provenance`/`source`, semver compare, discover + install (copy) + update-when-newer, built-in vs user libraries. No external marketplace. Covered by `test_catalogue.py` (6). |
| **Skill registration gate** (`ack.gate`) | **wired + tested** | No unchecked skill registers: a **tool** must pass a doctor preflight (loadable, `CASE`+`capability`, **sync** `run`, derivable schema) and ship an auto-generated test; a **playbook** must have valid `SKILL.md` frontmatter + readable references + a passing `scripts/check`. Behavioral `eval/` stays opt-in. The **generation-completeness gate** `gate_generated` (#601 S11) is stricter still — it **rejects** an unfilled scaffold (the `ACK-SCAFFOLD-SENTINEL` marker the generator emits into a fresh stub) and can **execute the sibling test hermetically** (`run_sibling_test_hermetic` / `gate_generated(execute=True)`: scrubbed env / hard timeout / isolated tmp; opt-in); exposed on the SDK surface. The loader **enforces** it at load (an unfilled scaffold in the active project's library is dropped), and `library_items_complete(library_root)` gates a whole per-project library (for the self-dogfood acceptance / an operator). The prompt gate has a **strict-locale variant** `gate_prompt(strict_locales=True)` (#601 S11b-1b): a declared non-source language whose `locales/<lang>.json` overlay is absent is a failure ("declared == delivered"; the lenient English-fallback gate stays the default), and `library_items_complete` applies it to every generated `kind: prompt` item in the library. Covered by `test_gate.py` (17) + `test_gate_generated.py` (6) + `test_hermetic_test_exec.py` (9) + `test_library_completeness.py` (8) + `test_doctor_validate_tasks.py` (4). |
| **Shared content i18n** (`ack.i18n`) | **wired + tested** | Core file-overlay locale loader (`Localizer(locales_dir)`): English source + `<lang>.json` overlay along a dotted path, English fallback; **flag-independent** (always importable, no `GX10_MPR`/plugin coupling), **parameterized locales dir** (each skill/prompt points at its own). MPR migrated onto it (its `i18n.py` is now a thin shim; 386 green). Distinct from `engine/messages.py` (engine chrome). Covered by `test_i18n.py` (6). Part of the core-always-on rebuild (ADR-0002, #112). |
| **Prompt library & generator** | **wired + tested** | Epic #105 on the core base ([ADR-0003](adr/0003-prompt-library.md) + [`prompt-packaging.md`](prompt-packaging.md)). The `kind: prompt` item format (`ack.prompt`, `test_prompt.py` 7); **multilingual assembly** (`ack.promptgen.assemble` — template + values → target language via `ack.i18n`, source/target + fallback; `test_promptgen.py` 6); **slash surface + guided elicitation** — a discovered prompt is exposed as the `use_prompt` engine tool (list → ask-next-required-question → assemble + preview, `lang`-aware), wired into `_load_skills`/`_effective_tools`/dispatch (`ack.promptgen.run_prompt`; `test_prompt_cmd.py` 9, #110); **eval/registration gate** for prompt items (`ack.gate.gate_prompt` — required vars must appear in the template, every declared language must assemble cleanly, overlays validated; `test_gate.py`) and a **curated multilingual starter library** of 11 built-ins under `skills/prompts/` (`code-review`, `commit-message`, `bug-report`, `explain-code`, `pr-description`, `refactor-plan`, `test-plan`, the product **`feature-spec`/PRD** authoring prompt (#1077), plus the **DEV-1 dev-process trio** `dev-process`, `verbatim-scope-audit`, `dev-loop-runner` from epic #532; EN+DE; `test_prompt_library.py` 33, #111/#150/#532/#1077). **New prompt = drop an MD file** — no engine change; or scaffold one with `/generate --kind prompt` (#601 S10c), which renders a gate-valid `kind: prompt` item into the active project's library. |
| **Discovery commands** (`/prompts`, `/skills`) | **wired + tested** | Read-only listing of the **one loaded registry** — no re-scan, no parallel mechanism. `/prompts` lists every loaded `kind: prompt` item (name, declared languages, description); `/skills` lists every loaded skill across both kinds — `SKILL.md` playbooks **and** typed `CASE`+`run` tools (incl. the MPR built-in). Both are server commands (forwarded by the clients), advertised in `/help`, and offered in the TypeScript client's slash autocomplete. Backed by `_catalogue_snapshot` over `_PROMPTS`/`_PLAYBOOKS`/`_PLUGIN_TOOLS`. Covered by `test_discovery_cmds.py` (6). |
| **Catalogue endpoint + dynamic autocomplete** (`GET /catalogue`) | **wired + tested** | A guarded read-only endpoint serving the loaded registry snapshot (same `gx10._catalogue_snapshot`, one surface) — gated like `/tasks`/`/doctor` (in `GATED_PATHS`; 401 without the deployment secret under token/sealed). The TypeScript client fetches it lazily on the first slash-menu open and merges the loaded **prompt** names into slash autocomplete as directly-invocable `/<name>` entries (a built-in command wins on a name collision; skills are discoverable via `/skills` but not injected as dead completions). Fail-soft: no `/catalogue` (older server) or a gated/closed session → built-in commands only. Covered by `test_catalogue_endpoint.py` (3) + `catalogue.test.ts` (6). |
| **Prompt invocation** (`/<prompt-name>`) | **wired + tested** | Deterministic, model-free per-item invocation: the command router resolves `/<prompt-name>` against the loaded prompt catalogue **after** every built-in command (so a real command never shadows), parses `var=value` / a single positional / `--lang xx`, and runs the `ack.promptgen` elicitation state machine — assembling the finished prompt in the target language when all required vars are present, else returning the guiding questions for what is missing. The model-elected `use_prompt` tool stays available (additive). No generic `/prompt run X` command — items stay `/<name>`. Covered by `test_prompt_invocation.py` (14). |
| **Skill-generation engine & library** | **wired + tested** | Spec'd in [ADR-0001](adr/0001-skill-engine-and-library.md) + [`skill-packaging.md`](skill-packaging.md). All parts ship: two skill kinds (typed `CASE`+`run` tools + `SKILL.md` playbooks), the `ack.skillgen` generator, the `ack.catalogue` library (semver + provenance), and the `ack.gate` registration gate. **`mpr` is the reference built-in** — now a **core always-on** built-in at `skills/mpr` (no `GX10_MPR` load gate; runtime `mpr.enabled`, default ON), consuming the core registry + `ack.i18n` + catalogue + gate; its `CASE` carries the catalogue manifest, indexed by `ack.catalogue`. Back-compatible (386 tests green, behavior unchanged when enabled; now part of the core `pytest` suite). The full lifecycle is verified end-to-end for both kinds (`test_skill_e2e.py`): generate → gate → register → load → invoke. |
| **Pluggable code-agent CLI** (`GX10_AGENT_CMD`) | **wired + tested** | The local code-agent the client launches per handover is a **command template**, not hard-wired to Claude Code — set `GX10_AGENT_CMD` to plug in any headless coding CLI with no code change (placeholders `{bin}/{model}/{effort}/{permission}/{prompt}/{feedback}`; `{permission}` defaults to `acceptEdits`, `{feedback}` is the optional result-capture path). **Claude Code and Codex (`codex exec`) are verified** backends; Codex drops `{effort}`/`{permission}` and uses `-o {feedback}` for the hybrid result capture. See [`code-agents.md`](code-agents.md). |
| **Config-driven code-agent registry** (`code_agents.pool`, #449) | **wired + tested** | Which agents EXIST (their identity, model + command shape) is a config-driven, always-on registry — `config.code_agents.pool`, a `providers.CodeAgentRegistry` keyed by `agent_id`, SEPARATE from the fan-out `providers.pool` (so it is independent of `providers.enabled`). Ironclad ships **OPUS**/**SONNET** as overridable defaults; `conf/` adds others (e.g. CODEX). `agent_id` is a letters-only filename token (round-trips both the handover + feedback filename regexes); an **unknown agent fails closed** at every gate (`stage_handover`/`advance_pipeline`, the autopilot reconciler, the server pull) — no silent default, no legacy alias. The handover `agent` schema enum is generated from the live registry. The **server resolves the full agent spec** (`bin`/`cmd_template`/`model`/`effort`/`permission`) into the `/pending` item; the client only renders it (one shared `build_agent_argv`). At boot the server **probes each enabled agent** (prompt-free; `bin` via PATH else the spec's private `bin_glob` newest-by-mtime) and is cli-available iff at least one resolves (#451). **`GET /coders`** (guarded) + the `/coders` client command show which coding agents are **bound** (registry + boot probe) plus the fan-out provider lane (`dispatcher.snapshot()`: pool reachability + last route reason + running budget); `/health` carries a compact `coders: {bound,total}` for the 2s poller (#452). During a fan-out the orchestrator emits a typed `[agent] <id> · <reason>` control frame per routed provider (the `[perf]` line-protocol pattern); every client parses it into the footer as a **live "which coder is being called" indicator** (#453). **`/coders use <id>`** pins which coding agent runs ALL handovers at runtime (`/coders use auto` clears it) — a `code_agents.pinned` override applied at every execution/reconciliation seam (`_effective_code_agent`), guarded `POST /coders`, fail-closed on an unknown agent; no pin ⇒ the orchestrator's task-chosen staged agent (#454). An out-of-budget agent is detected by a **server-side layered classifier** (JSON→stderr→exit, conf patterns; the client uploads the raw run signal) → a **process-lifetime circuit-breaker** trips and `_effective_code_agent` **fails over to the cheapest non-tripped capable peer**; `/coders` shows tripped agents, pinning one clears its breaker (#455). The failover is **task-class-scoped**: the class is derived deterministically from `task_json.type` (`gx10._task_class`; no model self-report trusted) and a `code_agents.classes` capability map restricts the peer search, so a budget-exhausted Opus on a **security**/**architecture** task never falls to a weaker agent (the staged pick + an operator pin stay authoritative; unmapped class ⇒ no restriction; all-capable-tripped ⇒ keep the chosen one) (#456). A code agent can be **onboarded while `enabled:false`** — inert (never offered/probed/launchable/failed-over-to) but shown in `/coders` as `(onboarded · disabled)` for operator visibility, the seam for a new backend pending exhausted-signal calibration (#460). See [`code-agents.md`](code-agents.md). |
| **Runtime contract self-check** (`GET /doctor`) | **wired + tested** | The same preflight the doctor CLI runs, exposed live so contract/registry drift surfaces at runtime (error/warn counts + a boot summary). Includes opt-in lodestar checks when enabled. |
| **Dev environment** (`Dockerfile.dev` + `docker-compose.dev.yml`) | **built (build+test gate)** | A reproducible, isolated dev setup that **builds the engine and runs the full suite inside a container** — the green build+test artifact is the precondition to promote. Resource-limited dev orchestrator, `open` profile, endpoint defaults to the reference stack and is overridable. Not needed to merely *use* Ironclad. See [`dev-environment.md`](dev-environment.md). |

## Memory

The engine has long-term memory **wired in**: a `query_memory` tool, store-on-task-
completion, and stage-time context injection, backed by `engine/memory.py` — a small,
secret-free client for a **Mem0-style HTTP service** (`POST /add`, `POST /search` with
`graph=false`, `GET /health`). Store + search are verified live against the reference
Mem0 stack (Qdrant + Neo4j + BGE-M3, LLM pointed at the local model).

**Three tiers (all server-side, fail-soft).** *Hot* = the model window (a bounded working
set, **token-accurately** trimmed against the model wall — real token counts from the served
model's tokenizer, with a calibrated chars/token fallback). *Warm* = `engine/warm.py`, a BSD-licensed in-memory store
(`GX10_WARM_URL`, e.g. Valkey) holding the rolling conversation summary + recent-turn state — which
**survive a restart** and are **shared across the reasoning workers** — plus a separate short-TTL
(~3 min) retrieval cache-aside. The shipped compose defaults `GX10_WARM_URL` to the `mem-valkey`
loopback, so the warm tier wires automatically under `--profile memory` (fail-soft otherwise). *Cold* = the Mem0 vector(+graph) store below. On top of the tiers:
rolling/hierarchical summarization on eviction (raw archived to cold first, then a **bounded
tail-first** summary input so the summarizer can't itself overflow the window, prefix-stable, and **query-aware** — the summary biases retention toward the current task, fail-soft to a generic instruction when no turn is in scope; a **default-off** proactive accountant sheds the oldest rounds early via summary once ingestion crosses a soft mark, bounded by a shared per-turn summarize rate-limit, #1051),
per-turn RAG assembly (warm cache-aside, the RAG block itself token-budgeted), token-accurate
budgeting (real token counts via the served model's tokenizer — the vLLM `/tokenize` endpoint —
with a calibrated chars/token fallback; the model window is auto-adopted from `/v1/models` at boot)
that scales the working set to the model window without overflowing it, a **single ingestion choke-point cap** (every `read_file`/`list_directory`/`search_files`/`execute_command` result — and the local-tool bridge return — capped to the live per-turn budget at one place, #1046, so no one tool result can overflow the window; structured/already-budgeted payloads like `web_search`/`parallel_reason`/MPR/memory are never touched), **ranged/pattern `read_file`** (#1047 — `start`/`end` line numbers or a regex `pattern` window, mirrored in the ink client, so the model reads only the relevant slice of a large file instead of the whole thing; a bad range falls back to the head+tail cap), a **pre-flight overflow guard** (before each call, reserve output —
`generation.max_tokens` / `GX10_MAX_TOKENS`, default 8192 — + the tools schema + the conditional
thinking budget; the output reserve is an **adaptive ceiling**, not a fixed floor: when the full reserve
would not fit, it reserves **less** output — down to `context.min_output_tokens` / `GX10_MIN_OUTPUT_TOKENS`
(default 1024) — so a marginal-overflow turn proceeds **losslessly** with a shorter answer instead of
failing; a `context.overflow_safety_tokens` / `GX10_OVERFLOW_SAFETY` headroom (default 1536) is kept below
the wall so the clamp never targets it to the token — absorbing the gap between the engine's estimate and
vLLM's exact rendered-prompt count (template + tools serialization); only when even a minimal answer will
not fit does it emergency whole-round trim, then **iteratively truncate the largest turns' content** — so an
agentic loop (one user turn, many accumulated tool reads, no round boundary to evict) degrades to head+tail
excerpts rather than dying — and the discarded slice is **cold-archived** (`fragment_trim`) so B2 RAG can re-inject it query-aware next turn (#1050; an optional **default-off** `emergency_summarize` replaces the raw drop with a bounded summary under a hard timeout, fail-through to raw) — then, last, a clear `ContextOverflowError` instead of a raw vLLM 400),
chunked + recency-ranked long-artifact storage, an on-demand
`deep_query_memory` graph tool, and worker memory (workers read the shared summary +
per-item retrieval, a single-writer reducer consolidates their writes). Each piece is
individually flag-gated (default-on where a memory/warm service is configured); with the
warm/cold tiers down a turn still completes. The hot read path stays vector-only and off the
timeout-prone graph path.

**This repo ships the wiring, never any memory content.** Memory is a runtime service,
not data in the codebase:

- **Off by default.** With no `GX10_MEMORY_URL` (and no `conf/memory/memory.json`) the
  `MemoryManager` is never constructed → all hooks stay inert and no tool is offered.
- **Bring your own service _and_ corpus.** Point the engine at your Mem0 endpoint
  (`GX10_MEMORY_URL=http://your-mem-host:8800`, optional `GX10_MEMORY_AGENT`). The store
  starts **empty** — Ironclad accumulates its own memory from task completions; any
  pre-existing corpus is yours to import, into your own namespace, and never lives here.
- **Namespace.** Reads/writes use `agent_id` (default `ironclad`) and an optional
  `user_id`; set them to match wherever your content lives.

So the public artifact is **wired but empty** — the integration works, the content is
the operator's.

## Rebuild punch-list — done

1. ~~Wire the engine to the Mem0 backend.~~ **Done** — `engine/memory.py`, off by
   default, ships empty (see Memory above).
2. ~~Port the autoplan loop into the server's queue consumer.~~ **Done** —
   `_autoplan_tick`, decoupled from autopilot, backlog-config-gated.
3. ~~Wire remote turn cancellation across the HTTP boundary.~~ **Done** —
   `POST /cancel` + Esc/Ctrl+C in the recommended client (Ctrl+C in the legacy TUI).
4. ~~Decide the grammar hard-floor.~~ **Decided** — verified stable on the reference
   GPU; available via the ACK; the engine keeps the soft path by design (see table).

The rebuild's known placeholders are now resolved. What remains is ordinary
hardening and breadth of testing — treat `main` as a development snapshot still.

## Extension & self-maintenance — what's done vs in development

Ironclad is meant to be **extended and maintained through itself**, with a clean DEV/Prod
split. Honest split of that capability:

- **Done (wired + tested) — the user-facing surface.** The **open plugin API**
  (`GX10_PLUGINS_DIR`, [`plugin-api.md`](plugin-api.md)), the **pluggable code-agent CLI**
  (`GX10_AGENT_CMD`, [`code-agents.md`](code-agents.md)), the **dev container** build+test
  gate ([`dev-environment.md`](dev-environment.md)), the runtime `GET /doctor` self-check,
  and the beginner **self-maintenance guide** ([`self-maintenance.md`](self-maintenance.md))
  all ship and are covered by tests (`test_plugins.py`, `test_client_pool.py`,
  `test_doctor_endpoint.py`). The extension model is *additive*: plugins dock onto a stable
  contract, the core is never *forked* — plugins dock through the stable contract and `core/` stays standalone + secret-free (boundary-check enforced; the check guards `core/`, it does not sandbox plugin code).
- **Mostly done — our internal release machinery.** The **DEV → Prod → Public promote** now
  runs as a **single fail-closed gated command**: boundary (core + client) → the full test suite
  (with a JUnit proof) → docs gate (CHANGELOG + doc-reality-audit) → export gates → a
  **PRE-publish clean-room** (wheel → fresh venv → import-smoke → an example plugin builds against
  the installed SDK) → a 2nd-instance review → publish → prod redeploy. It is **dry by default**
  and stops before anything irreversible. The remaining step is **full automation** (a scheduled
  evening sync). Downstream users never touch any of this — they pull the framework and extend it
  over the plugin API, or fork it freely (Apache-2.0).

So the way you *extend* Ironclad is shipped and tested; the way *we harden our own releases* is a
single gated flow, with scheduled automation the last step.

## Reference load test

Run against the full reference stack on the DGX Spark (2026-06-17): all five
containers up — `vllm-35b`, `mem-api`, `mem-qdrant`, `mem-neo4j`,
`ironclad-orchestrator` (all healthy) — driven from a workstation over the LAN.
*(The warm tier `mem-valkey` was added after this run — `--profile memory` now brings up **five**
containers: the four `mem-*` services + the always-on orchestrator; `vllm` is a separate
`--profile model`.)*

**Fan-out (8 independent reasoning prompts, concurrent):**

| metric | value |
|--------|-------|
| success | 8 / 8 |
| wall-clock | 1.2 s |
| Σ per-prompt latency | 7.1 s |
| **speedup vs serial** | **5.8×** |
| aggregate throughput | ~118 tok/s |

**Chat (3 sequential turns, single agent — serialized by design):**

| metric | value |
|--------|-------|
| mean turn latency | 2.1 s |
| decode rate | ~55–68 tok/s |
| answers | all correct, in the configured language (German) |

Interpretation: the parallel reasoning path scales near-linearly to the model's batch
width (`max_num_seqs=8`); the conversational path is intentionally one-turn-at-a-time
behind the agent lock. Both are healthy on a single GB10.

## Sealed channel — live verification (Phase d)

Verified on the reference GPU (2026-06-17) against a server in the `sealed` profile,
driven from a workstation over a **real SSH local-forward** (the client-managed tunnel),
without touching the production orchestrator:

| check | result |
|-------|--------|
| server bind | `127.0.0.1:8101` only (loopback — never the LAN) |
| `/health` | `sealed: true`, `security.profile: sealed` |
| `/tasks`, `/session/open` without the secret | **401** |
| `/session/open` with the secret → `/tasks` with secret + session | **200** |
| real model turn (`/chat`) through the sealed tunnel | answered correctly |
| after `/session/close` → `/health`, then `/tasks` with the stale session | `sealed: true`, **401** |

The channel is open exactly while a live session exists and seals the moment it ends —
OS-enforced when the tunnel closes, app-enforced by the session heartbeat. Single-tenant
throughout: the secret authenticates the deployment, not a user.
