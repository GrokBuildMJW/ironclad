# Test report

> Maximum transparency: this is the actual state of testing, including issues found
> **and fixed** during the campaign. Counts re-confirmed **2026-07-15** (offline suite; live
> verification 2026-06-17). For per-component wiring status see
> [`status.md`](status.md); for what's planned see [`roadmap.md`](roadmap.md).

## Summary

| | |
|---|---|
| Automated tests (offline, no model) | **3576 passed** |
| Live smoke tests (skipped without a model) | **9** |
| **Total Python** | **3585** |
| TypeScript client tests (`node:test`) | **481 passed** (485 total, 4 skipped) |
| Full agentic loop, end to end, with a **real** code-agent | **verified** |
| Issues found during the campaign | **1 functional gap + 5 review findings — all found and fixed** (see below) |

All offline tests run with no network and no model (the OpenAI client and heavy deps are
stubbed), so they are deterministic and fast (~24 s). The live suite is **skipped by
default** and only runs when pointed at a real server.

## How to reproduce

```bash
# 1) offline suite — deterministic, no model needed
pytest -q                                   # → 3576 passed, 9 skipped

# 2) live smoke — against your own running orchestrator
GX10_LIVE_URL=http://<your-host>:8100 pytest -k live -q     # 9 passed
# (set GX10_LIVE_TOKEN too for the token/sealed profiles)
```

## Coverage by area

The breakdown below groups the suite by capability area and sums to
the **3585** total (3576 offline + 9 live). It is a high-level, per-area view of QA
coverage — a full test-name-by-area mapping is not reproduced here.

| Area | Tests |
|------|-------|
| **Completion authority end-to-end** — punctuation/quote normalization with strict near-miss refusal, the shared exit-zero stamp used identically by local and server lanes, real `/feedback` ingestion followed by store transition checks for clean/prose, failed/unknown, blocked, clarification-needed, and explicit-done results, plus the Python-client first-line prompt contract | 14 |
| **Agent-Contract-Kernel** — schema SSOT, validate→reask (incl. malformed-tool-call history + loaded-session sanitisation), escape-free authoring (write_last_reply + write_file append), capability-detected forge issue-filing (create_issue — label validate→reask against the repo's real labels + optional `parent` native sub-issue linking) + forge issue-reading (view_issue — resolve a `#NNN` reference via the tracker, authoritative NOT_FOUND), SSRF-guarded fetch_url, targeted edit_file, the mandatory tamper-evident per-action audit ledger — intent-before-dispatch fail-closed, result append + surfaced degraded-health latch through refusal and recovery, full-surface who/what/when/why, and audit-dir tamper-resistance, the mandatory fail-closed untrusted-result injection fence, the mandatory fail-closed model-command OS sandbox (bwrap/firejail on POSIX; Windows refusal), constrained emission, capability registry, the gh-independent native forge adapter seam (cli|native|mock; the forge tools work with no gh on the box), open-PR create_pr (open-only), issue-commenting comment_on_issue (narrow), PR CI/mergeability snapshot pr_status (non-blocking), the epic tracker record type (TaskType.EPIC — never capability-gated, grammar-clean; plus the edit_file no-op guard that refuses a byte-identical edit) + the generic cross-model second-opinion `review` tool (registry-resolved reviewer, mocked `default_cli_runner`, diff-mode + paths-mode, capability gate, agent selection + anti-affinity, ingestion membership), plus the product dependency-closure egress analyzer (`ack.egress` — pinned-lockfile closure resolution + a bundled versioned known-egress dist set, ecosystem-namespaced allow/deny with bare cross-ecosystem entries, deny-by-default block for a `network:none` design; plus the advisory static import/socket egress scan; plus the sandboxed build/test hermeticity probe (engine.egress_runner, --net=none, platform-honest advisory where no backend); plus the Rust Cargo.lock dependency-closure egress analyzer (`ack.egress.rust_deps` — concrete known-egress crate set, feature-gated crates via injected cargo-metadata features, ecosystem-namespaced allow/deny; plus the Rust advisory static import/socket egress scan; plus the Rust two-phase hardened hermetic build probe (engine.egress_runner, env-neutralized net-allowed cargo fetch → sandboxed --net=none build, block only on the sandbox net-deny signal; plus the Rust multi-ecosystem dispatch — per-ecosystem isolation (a Rust-analyzer bug never nulls the Python findings), ecosystem-tagged Rust findings, and the safe offline cargo-metadata feature resolver), ADR-0008/0009); plus tool-schema requiredness follows the SIGNATURE not the annotation's nullability (`derive_tool_schema`) — an Optional-typed param with no default stays required and its value schema keeps the null arm (matching pydantic's `anyOf` Optional rendering), so a required `int | None` no longer lets the model emit `{}` into a missing-argument dispatch crash | 373 |
| **Failure classification** — the shared, string-valued `FailureClass` taxonomy SSOT + the deterministic, rule-based validated-emit classifier (the additive, advisory `ValidatedEmitResult.failure_class`; byte-identical no-op on success) + the providers→enum bridge re-mapping the 3-class run taxonomy onto one enum + the absolute never-raises backstop, plus the **always-on code-agent-failover production**: `gx10._record_failure_class` classifies every failed run at the server feedback path and surfaces it (`gx10._last_failure_class()` + a `failure_class` response field) for the finite Strategy consumer | 31 |
| **Lesson distiller (provider)** — the project-private `EngineLessonStore` `ack.lessons` provider: scope-isolated persistence + recency/query ranking + typed categories + compaction + scope-priority `brief` + `forget` purge + robustness (corrupt/missing/weird-scope; malformed/overflow-cap safety; empty-scope-guarded `by_category` + `brief`; fail-soft on invalid-UTF-8/hostile-dunder/hostile-str-scope+lesson/hostile-category/bad-base-dir/save-failure). The `EngineLessonStore` class remains as the legacy backend + the ACE migration source; its `lessons.enabled` engine wiring is **superseded** by the always-on ACE PlaybookStore (see *ACE wiring* below) | 38 |
| **Strategy revisor** — the pure `revise(failure_class, attempt, budget) → Strategy` policy SSOT (per-class action snapshot, totality, spent-budget escalation, no-raise), the optional `validated_emit` `strategist` re-ask seam, and the engine `providers.code_agent_strategy` application, plus the **always-on engine failover consumer**: `gx10._revise_on_failure` counts each per-task failure, persists terminal `blocked_kind="escalated"` when the bounded budget is spent, and stops feedback/pending/reconciler failover; a success resets the counter. The retired `strategy.enabled` values warn but cannot disable it | 30 |
| **Loop profiles** — the pure `resolve_loop_profile` per-TaskType deep-merge SSOT (default/by_type precedence, present-key override, retry-budget clamp, floors, enum-value key, never-raises) + the engine `_loop_profile` accessor driving the chat-loop bound (byte-identical default incl. a zero-iteration fallback, override pickup, run()-wiring guard) + per-profile `eval_verifiers` activation, plus the **per-TaskType failover budget**: `gx10._failover_budget` resolves `loop_profiles.by_type[<type>].retry_budget` (layered over `strategy.budget`, clamped to the hard ceiling) for the code-agent failover escalation — the first live `by_type` consumer on the dev-task pipeline | 26 |
| **Verifier / evaluation** — `VerdictResult` plus deterministic `verify_rules`, advisory `verify_grounding`, and the separate async budget-gated `verify_with_judge`; engine coverage now includes the always-on synchronous required-rule staging gate, exact failed-rule refusals, verifier-unavailable fail-closed behavior, zero-mutation create refusal, and advisory grounding that reports unavailable separately from passed before feeding Quality. `verify.enabled` is a tombstone. | 34 |
| **Quality circuit breaker** — the separate `QualityBreaker` trend logic plus always-on engine construction and `post_handover` score feeding; a sustained latched trip now synchronously holds both create and re-hand before any task/handover write and emits escalation. Coverage proves legacy `quality.enabled` values cannot remove the breaker/consumer and tuning preserves breaker state. | 24 |
| **Process self-correction** — the pure `distill_process_lesson` (success→working-path, missing-clarification priority, none-when-not-actionable, never-raises) + `format_process_hint` (render/empty/limit/never-raises) + the OPT-IN engine wiring (`_record_process_lesson`/`_process_hint` roundtrip, byte-identical when disabled / no concrete provider / string-only provider / empty scope / overflow max_hints, never-raises on garbage). The `post_feedback` Process-SC consumer is now **subsumed** by ACE's reflect→curate — its synchronous write is retired, but the TYPED read surface survives: `_concrete_lesson_provider` is duck-typed so `_process_hint`/`record`/`by_category` keep working against the ACE PlaybookStore (proven in *ACE wiring* below) | 17 |
| **Loop-Intelligence Hook-Bus** — standalone fail-soft observer dispatch across the eight lifecycle/escalation events, copy-on-write registration, cancellation/budget behavior, and engine publish-point wiring. Coverage confirms required verifier/ambiguity decisions occur directly at staging rather than through observer hooks, while the always-on `post_handover` Quality feed and optional escalation notifier remain consumers. | 31 |
| **ACE playbook (evolving context)** — the Loop-Intelligence → ACE (arXiv 2510.04618v3) data-model foundation `ack.ace.playbook`: an itemized `Bullet` (stable id + helpful/harmful usage counters + tags) in a sectioned `Playbook` (the four canonical ACE sections + custom), a human + machine-parsable `render()` whose STABLE `[id] content #tags` prefix precedes the MUTABLE `(↑helpful ↓harmful)` counters (KV-cache stable prefix), and a versioned, lossless JSON round-trip (ids/counters/tags/seq preserved; newer schema refused fail-closed); pure/stdlib-only, ships in the wheel as `ack.ace` and is the always-on core's data model | 17 |
| **ACE Reflector** — the Loop-Intelligence → ACE role `ack.ace.reflector`: distils reusable insights from a reasoning trajectory + natural execution feedback (label-free) and rates used bullets helpful/harmful/neutral; structured Trajectory input + ReflectorOutput, optional iterative refinement (default 1 round); injected `chat` transport (stdlib-only), fail-soft (transport/parse error → empty, never raises) | 8 |
| **ACE Curator** — the Loop-Intelligence → ACE role `ack.ace.curator`: integrates Reflector insights into the playbook as a compact delta (`{reasoning, operations}`) merged DETERMINISTICALLY (no monolithic LLM rewrite); `curate` builds section-validated, localized add/rate ops, `apply_delta` merges in place; deterministic + pure/stdlib-only, robust (missing bullet / unknown op / empty content skipped, never raises), `Delta.from_json` parses an externally-emitted delta | 8 |
| **ACE grow-and-refine** — the Loop-Intelligence → ACE refinement `ack.ace.grow`: de-dup of near-duplicate bullets within a section (semantic via an injected batched embedder, threshold 0.9, with a lexical token-Jaccard fallback + fail-soft on embedder error) and utility-based pruning to a bullet-count/rendered-size budget (lowest helpful−harmful first), run proactively or lazily; merging folds counters + unions tags; pure/stdlib-only. Prune tiebreak evicts the OLDEST bullet on a utility/harmful tie (a fixed sign error in `-_seq` had otherwise evicted the newest) | 10 |
| **ACE Generator integration** — the Loop-Intelligence → ACE Generator side `ack.ace.generator`: playbook-guided inference via relevant-bullet retrieval (semantic over an injected embedder + lexical fallback, fail-soft — only the relevant subset enters the prompt, resolving the 32k window), bullet-id usage tracking, and the output→Reflector `Trajectory` closure; pure/stdlib-only | 8 |
| **ACE online adaptation** — the Loop-Intelligence → ACE closed loop `ack.ace.online`: `adapt_once` composes Trajectory→reflect→curate→apply_delta→refine over the playbook in place (label-free, cumulative, budget-gated via an injected ledger, fail-soft, no-op when nothing learned); `ReflectionWorker` is the async seam (O(1) non-blocking submit + background daemon — the hook never runs the model inline; drops on full queue, fail-soft on a bad item); pure/stdlib-only. **The robustness half now runs INSIDE the live adapt loop** — after refine, `adapt_once` runs `resolve_contradictions` (keep the higher-utility of a contradicted same-section pair) + `quarantine_noisy` (drop net-negative bullets), gated `AdaptConfig.robust` (default on), reported as `resolved`/`quarantined`; so `ack.ace.robust` is no longer orphaned | 13 |
| **ACE offline adaptation** — the Loop-Intelligence → ACE batch build `ack.ace.offline`: `build_offline` optimizes a playbook over a dataset across **multi-epoch** (default 5) **batches** (base size 1) — per-sample reflect→curate deltas computed independently (an injected `map_fn` may parallelize) and **merged deterministically** (`merge_deltas` — input-order, last-wins ADD collision, RATE/TAG/REMOVE accumulated), then applied + refined; budget-gated, **label-free** (labels only for eval), fail-soft per sample. `evaluate` is the test-split **pass@1** (uses only the optimized playbook, not the training data); `warmup` is the label-free execution-ledger replay that seeds the playbook the online loop continues on. Pure/stdlib-only. **Now reachable via `/ace warmup --ledger <path>`** (`_ace_command` -> `PlaybookStore.warmup`) — offline warm-starts the active scope's playbook from a dev-loop ledger's historical trajectories (read as plain data, boundary-clean); opt-in, off-hot-path, fail-soft, no-op without a model/scope | 14 |
| **ACE evaluation (shipped metrics + baseline harness)** — the Loop-Intelligence → ACE eval surface `ack.ace.evaluation`: the SHIPPED metrics — `accuracy` (exact predicted==ground-truth %), `goal_completion` (TGC / SGC, fraction successful + per-difficulty split) — and the comparative-baseline cost/latency harness: three BUILT, `RolloutMeter`-instrumented adaptation strategies (real ACE `reflect→curate→merge`, a monolithic full-rewrite baseline, an evolutionary validation-loop baseline) whose cost is **measured**, proving ACE does ZERO full-rewrites + ZERO LLM merges and cuts rollouts **>50%** vs the evolutionary baseline; `kv_cache_metrics` measures the stable-prefix cacheable ratio of successive renders (append-only ⇒ high reuse, a rewrite ⇒ low); `validate_epochs` is the guard (≥1, default 5); `EvalReport` aggregates the surface. Pure/stdlib-only, fail-soft metrics. **Now runnable on the LIVE system via `/ace eval --ledger <path>`** (`_ace_command` → `PlaybookStore.benchmark` → `compare_adaptation` over the dev-loop ledger's real trajectories → the efficiency verdict) — a pure measurement (no playbook mutated), so ACE's efficiency claims are demonstrable on the real workload, not just synthetic fixtures | 16 |
| **ACE robustness + safety** — the Loop-Intelligence → ACE robustness layer `ack.ace.robust`: graceful degradation under a weak/noisy reflector — `quarantine_noisy` drops harmful-dominant (net-negative) bullets so moderate noise is rejected without collapse, and `adaptation_gain` shows a weak reflector still nets positive **after** quarantine while a stronger one gains more (monotone); `detect_contradictions` finds opposite-polarity same-section bullets and `resolve_contradictions` keeps the higher-utility belief; `unlearn` is selective item-level forget by bullet id, no retraining; `version_id` + `diff_versions` + `PlaybookHistory` (snapshot/rollback) are the playbook versioning + reversible-rollback safety net. The bare modals "should"/"must" are NOT treated as negations (obligation, not polarity), so `you should X` vs `you X` is no false contradiction. Pure/stdlib-only, never raises. **`resolve_contradictions` + `quarantine_noisy` are now invoked in the live online adapt loop (`adapt_once`), not just a library**. **`unlearn`/`PlaybookHistory` are now operator-reachable** — the `PlaybookStore` persists a bounded per-scope version history and exposes `snapshot`/`versions`/`rollback`/`unlearn` (each mutating verb snapshots first so it is reversible; history survives a fresh store handle), wired to the `/ace` verbs. **Learned-state safety is always on**: `PlaybookStore.adapt` durably snapshots active state, adapts a deep candidate, and atomically promotes or quarantines it — with an injected scorer it promotes only a measured non-regression, and without one the built-in default gate promotes all normal learning (new bullets, helpful AND harmful ratings, a moderate prune) and quarantines only a catastrophically destructive adaptation (empties or loses >50% of the playbook); active state is never changed on a quarantine, the snapshot is the rollback net. `ace.safe_promote` is a warning-only tombstone | 35 |
| **ACE wiring (always-on engine core)** — the Loop-Intelligence → ACE engine integration `engine.playbook_store` + the gx10 wiring: the always-on `PlaybookStore` that SUPERSEDES the `EngineLessonStore` as the registered `ack.lessons` provider (NO enable flag) — implementing BOTH the string LessonProvider surface and the typed `record`/`by_category` Process-SC surface over the bullet playbook, plus the query-aware `context_for` (the 32k-safe Generator handover read), the budget-gated online `adapt`, persistence + the per-scope cap + the legacy-tree migration; the gx10 `_apply_ace` registers the provider + the `post_feedback` consumer that SUBMITS a Trajectory to the background ReflectionWorker (off the hot path, never inline) + starts the worker, `_concrete_lesson_provider` is duck-typed (Process-SC never silently breaks), a foreign provider is never clobbered (richer-wins / ACE steps back), the handover read-site injects `context_for`, and the memory-service `/embed` endpoint (BGE-M3) feeds semantic dedup/retrieval — fail-soft throughout (no model ⇒ no-op). `ace.top_k` is now threaded (via `configure`) to the `context_for` injection cap (was inert), and the supersede check is identity-based (`current is _ACE_STORE`) so a FOREIGN `PlaybookStore` provider is never clobbered. **Desktop functional test:** the Reflector chat adapter runs **thinking-OFF** (`enable_thinking:False`) with a ≥2048 budget — else a reasoning model (qwen3.6-35b) burns the cap on `<think>`, returns empty `content`, and the always-on loop learns nothing (live-verified: the fix makes ACE learn a real bullet on qwen); plus **transactional rollback** — `rollback` now honours the fail-soft persistence helpers' return values, so a write failure (read-only ACE dir / full disk) returns an error instead of a false success that would tell the operator a harmful learned playbook was removed while the active JSON is in fact unchanged | 32 |
| **ACE live feedback** — the live always-on hook now feeds the loop-intelligence machinery on the GENERAL task path: the handover injection site records WHICH playbook bullets it injected (keyed by task_id, bounded in-memory map), and the `post_feedback` consumer threads those **real `used_bullet_ids`** + a **real label-free outcome** — a fresh `OK: pipeline advanced` ⇒ `success`, an `ERROR: pipeline step failed` ⇒ `failed` (so the Reflector learns from FAILURES too and rates the used bullets on the live engine); already-done re-advances + trivial precondition errors are skipped; missing map ⇒ empty ids; off-path + fail-soft preserved | 8 |
| **ACE dev-process trajectory** — the pure `ack.ace.devtraj` ledger→Trajectory adapter: `ledger_to_trajectories` re-projects already-parsed dev-loop ledger payloads (the same boundary-clean ledger `lifecycle_projector` reads) into one ACE `Trajectory` per unit — driver transitions grouped by `unit`, abort by `abort`; ordered timestamp-free leg summaries (`GATE gate passed` / `REVIEW review-evidence passed` / `…FAILED: <reason>` / inert-review marked); a **label-free** outcome ∈ {`reached-human-merge-gate` (driver stopped at the human MERGE gate — not a confirmed merge), `aborted`, `blocked`, `in-progress`}; `used_bullet_ids=[]` (correlated separately). Pure/stdlib-only (no engine import), schema-drift tolerant, never raises; a DELIVER record (no `unit`) is skipped | 9 |
| **ACE dev-process learn-trigger** — the engine wiring `gx10._ace_scan_dev_ledger`: scans the dev-loop ledger OFF the hot path, projects it into per-unit Trajectories and submits each **newly-terminal** unit (reached-human-merge-gate / aborted — not in-flight blocked/in-progress) to the existing `ReflectionWorker` **exactly-once** via a persisted submitted-units set (survives restarts). The ledger is read as plain data (no private import); a chain-tampered/unreadable ledger is **skipped fail-closed**; no worker/scope ⇒ no-op. Wired into the `/lifecycle gate` command (the dev-loop's DELIVER-leg ledger touchpoint, reusing the payloads it read; advisory — does not affect the gate). Distinct per-UNIT signal from the per-handover post_feedback hook (run.py is a separate process → no double-learning). The exactly-once key is persisted per-item BEFORE submit (at-most-once — no re-learn on a crash between submit and a batched save). Covered by `test_ace_devwire.py` (8) | 8 |
| **ACE dev-process used-bullet correlation** — closes the used-bullet loop for the dev-process: the handover injection site (where the coder's handover gets the playbook via `context_for`) DURABLY records which bullets it injected, keyed by the engine task id **and** the issue#s the handover references (the standard `Closes #N` linkage — `gx10._ace_unit_keys`/`_ace_persist_injected` → `playbook_store.record_unit_bullets`); the ledger scan reads it back by the unit (issue#) to populate `Trajectory.used_bullet_ids` so the Reflector rates which dev-loop bullets helped. Proven for the **handover+feedback coder-addressing mode** (GitHub-only autopilot + file-based-with-handovers + public `ack.devprocess.stage_handover`→engine driver, where the injection site reaches the coder via the handover md it reads). The machine-gated dev-loop worktree-agent injection (no handover seam) is a tracked follow-up. `[]` when no correlation (weaker, not wrong); fail-soft. Only the FIRST title `#N` is the primary unit (no cross-attribution on a multi-issue title). Covered by `test_ace_devbullet.py` (6) + the `record_unit_bullets`/`read_unit_bullets` cache (2 in `test_ace_playbook_store.py`) | 8 |
| **ACE dev-process e2e proof** — the dev-process capstone: a boundary-clean END-TO-END test driving the full stack — ledger → `ack.ace.devtraj` → the gx10 ledger scan → the real `ReflectionWorker` → `PlaybookStore.adapt` → a real playbook mutation — for the SAME ledger schema both the **public** generic `ack.devprocess` driver AND the **internal** dev-process substrate emit, proving ONE adapter serves both. Also proves fail-closed handling (a tampered/absent/garbage ledger ⇒ no crash, no learning), **exactly-once** (a re-scan adds nothing; the per-UNIT merge-arc signal is distinct from the per-handover hook → no double-learning), and the used-bullet correlation flowing through (a used bullet is RATED helpful). Reads the ledger as plain data — imports nothing private (no deployment IPs, hostnames, or private product names) | 6 |
| **ACE fork signal** — the pure `ForkSignal` / `ForkResolution` contracts and ledger adapters plus precision-first `detect_ambiguity`; an always-on direct staging gate returns the signal question/options, refuses detector errors fail-closed, proves zero writes on a hit, and remains active when optional `ace.fork_mpr.enabled` is off. Retired config/env controls are tombstones. | 15 |
| **ACE MPR-at-fork panel** — the gated, off-hot-path MPR architecture-decision invocation: a new gate `ace.fork_mpr.enabled` (default **OFF** — gate OFF is byte-identical to today's STOP-and-ask) controls whether a recognized `ForkSignal` (parsed from the ledger by the fork-signal detector) fires MPR's `architecture-decision` panel via `run_tool('mpr_research', {domain_hint:'architecture-decision', mode_hint:'decision'})` on a dedicated `_ACE_FORK_WORKER` (ReflectionWorker reused as a generic off-path queue worker, so the multi-LLM-call panel never blocks the dev-loop/turn path). The MPR query is **pre-informed** by the playbook's prior fork bullets via `PlaybookStore.context_for`. Exactly-once (a persisted fork-key set); fail-soft on every mode (MPR off / no model / no initiative / budget / raises → no-op, the ask still surfaces). Wired at the `/lifecycle gate` ledger touchpoint (reusing the payloads the ledger scan read). Robustness: the exactly-once key is committed only AFTER the MPR run completes (a queue-drop / worker crash leaves it un-committed and retried, via an in-memory in-flight guard), and the fork worker is torn down on a gate ON→OFF flip. Covered by `test_ace_forkmpr.py` (9) | 9 |
| **ACE fork proposal surface** — the MPR matrix attached to the ask as a recommendation: the decision-matrix produced off-path by the fork panel is recorded as a fork-proposal pointer bound to the unit (`playbook_store.record_fork_proposal`, latest-wins, bounded), and `gx10._ace_fork_proposal_for(unit)` renders it — a GENERIC, boundary-clean seam BOTH dev-processes attach to their operator ask — as a **recommendation only**: MPR's top-ranked option (extracted from the synthesis, robust across phrasings) + the full ranked matrix + dissent + an explicit "this is a recommendation, NOT a decision; you decide, ACE learns from the choice". A no-op MPR result (disabled/declined/ERROR) is NOT persisted ⇒ the ask surfaces unchanged, no empty artifact (fail-soft). The OUTPUT leg is wired to the operator via the read-only **`/fork [unit]`** engine command (`_fork_command` → `_ace_fork_proposal_for` + `list_fork_proposals`), the production caller both dev-processes reach. Covered by `test_ace_forkpropose.py` (7) + the `record_fork_proposal`/`read_fork_proposal`/`list_fork_proposals` pointer (3 in `test_ace_playbook_store.py`) | 10 |
| **ACE fork-decision learning** — closes the propose→decide→record→pre-inform loop: a newly-RESOLVED fork (the `ForkResolution` on the ledger) becomes a fork-decision `ack.ace.Trajectory` (query = the fork's question — the stable comparability key; steps = chosen option + area; outcome = chose <option> → <outcome>; `used_bullet_ids` = the prior fork bullets that seeded the fork-panel query, captured `fork:<unit>`) submitted to the EXISTING `_ACE_WORKER` → reflect→curate writes a bullet in `strategies_and_hard_rules`, so the fork panel's `context_for` retrieves it at the next comparable fork. Gate OFF ⇒ no learning (byte-identical); exactly-once (a persisted decision-key set, distinct from the unit-arc + per-handover signals → no double-learning); fail-soft. Covered by `test_ace_forklearn.py` (6) | 6 |
| **ACE MPR-for-architecture e2e proof** — the architecture-fork capstone: a boundary-clean END-TO-END test of the whole gated fork propose-loop through the ONE core seam — a `ForkSignal` fires the gated, off-path, pre-informed MPR panel → the matrix is recorded + rendered as a recommendation → the operator resolves → a fork-decision bullet is recorded → a SECOND comparable fork's MPR query is pre-informed by it. Proven for BOTH dev-processes (public generic + internal) through the same ledger schema/seam; the **gate-off no-op** (no MPR, no proposal, no learning, no pre-informing — byte-identical); and the full **fail-soft matrix** (tampered ledger, MPR no-op/ERROR, MPR raises, malformed signal → the ask always surfaces, no crash, the loop never blocks). Covered by `test_ace_forkproof.py` (4) | 4 |
| **Closed-loop e2e** — the reflection loop proven LIVE end-to-end on the dev-task pipeline: a staged handover is scored (Verifier) → fed to the Quality breaker + trips → a run failure is classified (FailureClass) → the Strategy Revisor escalates on a spent budget; the optional reflection seams off are a no-op while the ACE loop-intelligence core is always-on and owns `post_feedback`; plus the `eval` per-type verifier selection | 3 |
| **Function-calling robustness** — tool-argument validation and model-agnostic call recovery | 24 |
| **Server / client split & security** — HTTP surface, canonical feedback task-ID validation + resolved-inbox containment, trust profiles, sessions, sealing, the config tree + runtime config, command router, doctor, catalogue endpoint, the server-side tool bridge, the coders / health observability blocks, the per-project code root shipped in `/pending` so a client-launched coder lands in the active project tree, and the client<->commands.py command-parity guard, plus the injected CLI-runner secret-env hardener (`engine/agent_env.py` — scrubs every secret NAME from the spawned coder's env + redirects the git/gh credential-discovery paths; `HOME`/`~/.claude` preserved). **Desktop functional test:** the captured `/chat` response strips the interactive status chrome (`[GX10]` / `[Qwen (planning)]`) so the API returns the answer, not pane markers; plus **no silent turn stall**: a per-request LLM timeout on every OpenAI client (`connection.request_timeout_s`), tool-loop cancel-honouring that never orphans a `tool_calls` message, and a bounded agent-lock acquire → 503 so a wedged turn can't block a second request forever; plus the **idle-turn watchdog**: a per-turn IDLE watchdog (`context.turn_idle_timeout_s`, reset on every generation chunk / completed generation / tool result — a slow-but-progressing MPR turn is never killed) that aborts a no-progress turn and **surfaces** it as a distinct `stalled` turn-end ("⏱ TURN ABORTED — model stalled"), never a silent hang; plus **client-facing markdown fidelity**: the engine's `_TableLineRenderer` re-emits pipe tables as PROPER GFM (pipes + inserted separator) and passes bold/code through, so the markdown-rendering client boxes tables and shows emphasis (was collapsing tables to pipe-less columns + stripping `**`); plus **tool-call display**: a Claude-Code-style header (the command/target via `_tool_display`, not the internal `execute_command(...)` chrome) + the FULL result under a `⎿` corner via `_tool_result_lines` (no 70-char cut; overlong output gets an explicit `… (+N more lines)`); plus **per-command shell routing**: `_detect_shell` sends a PowerShell cmdlet to PowerShell and a POSIX command to Git Bash (`_git_bash`) on a local Windows engine, and the runtime guidance opens up bash when Git Bash is present — both shells work; plus **deterministic listing count**: a shell listing (`ls`/`Get-ChildItem`, incl. `cd X && ls`) carries the same `N directories, M files` header as list_directory via `_directory_count_header` (from the FS, not by parsing output) + `_listing_count_header_for_command` (safe detection, no guess on pipes/globs/`-R`/>1 path); plus **shell-only listings**: `list_directory` is no longer offered to the model — a sampled tool choice flipped the transcript between `$ ls -la` output and a `[D]/[F]` list — while the handler, `/ls` and the bridge stay live; plus the **code-built listing reply**: a simple listing carries a machine `AnswerData:` payload (ONE fs snapshot with the header) that the SERVER renders into the final localized `Answer:` sentence — **command-gated in `run_tool` itself** (one structural site for every topology + caller: run-loop, `/tool`, `/ls`, API; the machine line never leaks and a non-listing command like `cat` is never rewritten), fs-computed sorted names, sanitized against filename injection (backticks + every line/paragraph separator → `?`), hardened listing detection (case-insensitive `-recurse`, PowerShell value-param guard), en/de templates, one authoritative language — which the model copies verbatim (composition dropped names and broke the one-sentence format); plus **`ls -lA` listing default**: the default listing shows hidden entries but not the `.`/`..` pseudo-rows, so the visible rows match the deterministic count; plus **coloured listings**: `ls -lA --color=always` emits ANSI SGR — the engine STRIPS it from the model context (clean text, char-accurate cap) while streaming the raw colour to the DISPLAY (a coloured line is sent unwrapped so its `⎿`/prefix stays plain and the client parser keeps the SGR); the renderer paints the native colours (sandboxed — colour only, no cursor tricks); the strip is scoped to execute_command (ingested file content is never altered), covers CSI+OSC+bare-Fe, and a non-coreutils `ls` that rejects `--color=always` is retried without it; plus cost-tier coder routing (task-type→tier derivation + tier-scoped failover; plus the tool-bridge frame that ships the active-project exec cwd); plus the **first-token-decoupled LLM timeout**: an opt-in `connection.first_token_timeout_s` (+ `connect_timeout_s`) splits the shared per-request float into a short connect deadline and a model-sized first-token/prefill read budget so a legitimate large-context reasoning prefill is no longer killed by the 120s read timeout, the per-turn idle watchdog is phase-aware (the generous first-token budget pre-first-token, the tight inter-token bound after) and closes a wedged stream on cancel, and a first-token timeout no longer re-issues the whole turn and surfaces a named actionable cause — all default-off / byte-identical; plus the **code-agent launch model guard + failed-coder surfacing**: an opt-in per-agent `models_probe` validates the configured model against the CLI’s advertised list at boot (WARN on mismatch) and gates a launch of an unadvertised model with a named error instead of a silent stall; failed/empty coder surfacing is always-on (a real failed coder that previously sat silently `in_progress` is now visibly blocked/errored), while only the model probe + launch gate are default-off / byte-identical when `models_probe` is unset; plus **bounded coder execution**: a live timeout shipped in `/pending`, dedicated process groups with whole-tree kill in both clients, older-server fallback, bounded Python stderr plus Ink stdout/stderr tails, and POSIX plus opt-in Windows descendant-kill proofs; plus **prompt tool-result delivery**: the Python client resends transient failures with capped exponential backoff until a 150-second default deadline (`GX10_TOOL_RESULT_DEADLINE_S`), reopens an expired session on 401/403, drops a stale 410 immediately, and drops other unrecoverable 4xx responses; plus a display-sink NUL scrub so attacker-controlled tool-result bytes can never forge a client `\x00TR…\x00` control frame; plus the per-connection socket deadline and stalled-body drop; plus a **busy `/chat/stream` returns a retryable 503** — the agent lock is acquired BEFORE the 200 stream headers (parity with `/chat`), so a wedged engine no longer answers a second streamed turn with a `[busy]` text chunk the client renders as a completed reply; plus a **hard whole-request wall-clock cap** (`_total_request_deadline_s` = first_token + request in decoupled mode, request otherwise) so a slow-drip stream that evades both the idle watchdog and the httpx read timeout can no longer hold the turn + agent lock forever — the upstream stream is closed and the generation ends with a named timeout, while a legitimate long first-token prefill keeps its full budget; plus the **engine-native coder launch is hardened to parity**: the autopilot/launch_coder lane now starts the coder in its own process group, bounds it by `code_agents.timeout_s` with a whole-tree kill on expiry that always releases the autopilot slot, and caps its captured log at 8 MiB while still draining stdout so a hung or newline-free coder can no longer hold the slot forever or grow disk without bound; plus the server-side tool bridge now observes cancellation — a bridged tool wait polls `_CANCEL_EVENT` instead of a bare 180s `Event.wait`, so `/cancel` (or a client that dies mid-tool without posting `/tool-result`) wakes the bridge and releases the agent lock right away instead of 503-busying every concurrent turn for up to 180s | 270 |
| **Provider-router / dispatch** — backend registry, routing policy, artifact routing, spill / fallback, setup-type resolution, reviewer anti-affinity, first-class web-search routing, and the **handover effort auto-tiering by task class** (security/architecture → xhigh, routine → high; explicit `effort:` wins; fail-open on an unmapped/unloadable class) | 104 |
| **Tooling envelope** — mandatory fail-closed `assert_authorized` at every coder-spawn site (`default_cli_runner`, handover, autopilot `_do_launch`, `review`, `/coders use`, both registry filters, and Ink), using the post-override `{bin, cmd_template}`. Omitted policy derives enabled CLI provider/code-agent tuples plus canonical Claude autopilot shapes; explicit-empty/malformed policy denies. Python/TypeScript share the matcher vector corpus, including Windows executable-stem fallback with byte-exact absolute pinning; tombstones cannot restore the old bypass. | 33 |
| **Web search & current-info routing** — the web-search tool gating + handler, the current-info intent classifier (English + German), the strict input contract + domain normalizer, the standalone adapter seam + a native HTTP adapter, the model-facing Sources formatter, the web_search prompt + tool-description, the sealed trust gate, the config + secret surface, the search-progress renderer, the 16-test spec consolidation, the tool-as-shell guard, and a fail-closed shell guardrail | 147 |
| **Memory & context** — Mem0 client, chunking, RAG, the rolling summary (query-aware), bounded summarizer input, deep query, vault reconcile, the warm tier, the token-budgeted handover brief, and the pre-flight overflow guard with its adaptive output reserve + safety headroom + iterative oversized-turn truncation, the single ingestion choke-point cap, the emergency-rung truncated-slice archive + optional summarize-not-truncate, the proactive ingestion accountant + shared per-turn summarize cap, ranged/pattern read_file, the file-heavy E2E repro keystone, the deliberate `remember` write tool, and the per-turn authoritative steering-state block (active project/unit/stage/task-counts/watcher-autopilot folded onto the user turn, single current copy, byte-identical when unbound) | 155 |
| **Lesson store / provider API** — the curated, versioned `ack.lessons` delegation seam: a `runtime_checkable LessonProvider` (get_lessons / report_lesson / brief) + set/get_provider; fail-soft no-op when no provider is wired (reads `[]`, writes no-op, a provider error never breaks a turn); a scope-priority `brief()` merge (provider-or-composed, dedup + limit); and the **fail-closed redaction-gated `promote()`** (a project-private lesson is promoted to a broader scope ONLY through an approving redactor). Lesson semantics are the provider's | 20 |
| **Lesson seam wiring** — the engine integration of `ack.lessons`: the handover read-site appends an advisory, scope-keyed lesson brief alongside the Memory brief, and the task-completion write-site reports the feedback as a scoped lesson (tagged with the task id) — both lazy-imported, both scoped to the active project/track `mem_scope`, and both byte-identical no-ops with no provider wired (the read returns nothing, the write does not even touch the feedback file), plus the **`post_feedback` Hook-Bus re-home** of the write site: the task-completion lesson write is driven by `gx10._lessons_consumer_hook` through the real `_advance_pipeline` wrapper (registered on **provider presence**, outside the vault lock), reporting on a fresh completion only (no double-report on an already-done re-advance), byte-identical no-op with no provider, fail-soft on a raising provider | 10 |
| **Scope-aware forget + scope tagging** — the substrate delete path: cold writes self-describe their origin `scope` in metadata (only when a project scope is bound — byte-identical for the base partition); `MemoryManager.forget(scope)` deletes a partition via the Mem0 `/delete_all` route (synchronous, fail-soft, fail-closed on an empty scope); the warm tier's `forget_scope(scope)` deletes the **exact**-scope session + retrieval-cache keys without cascading into deeper track scopes (fail-closed on an empty / glob-bearing scope); `ack.lessons.forget(scope)` is an optional, fail-soft provider verb; and `gx10._forget_scope` fans out across all three layers, fail-closed on an empty scope | 21 |
| **Memory-service scopes + orphan GC** — the partition-isolation guards + registry-keyed orphan garbage collection: the memory service's pure `require_scope` guard rejects an unscoped write/search (no `agent_id`) and refuses `run_id` as an isolation key; the engine's `MemoryManager.list_scopes()` lists the partitions present in the store (fail-soft), `_orphan_scopes` flags only **minted** `mem_ns` partitions with no registered project (never the base or a human-named scope, track sub-scopes judged by their project key), and `_reconcile_orphan_memory` forgets the orphans (dry-run by default, fail-soft per orphan; refuses to GC when the registry is unreadable), plus the **pure reflection-trigger policy** (`reflect_policy.reflect_decision`): the threshold-fire decision consumes the write counter at fire time and suppresses a fire while a reflection is already running — so writes during a (slow) run accumulate toward the next cycle (no undercount) and no bail-thread is spawned on the busy lock (no churn); never raises on a bad counter/threshold, plus the **curated-global tier helpers**: the pure `curate.promote_refusal` operator-gate (fail-closed — confirm + source scope + exactly one of redacted-text / source-query) and `merge_project_wins` (the project-wins fan-in: project results first + position-preserved, curated fills to the limit, dedup by text, provenance-tagged) | 30 |
| **Project track verbs** — the `/project track new|use|list` CLI: registry `add_track` (idempotent, fail-closed on an unsafe id / unknown project) + `set_active_track` (fail-closed when the track is unregistered), and the engine command that creates-and-switches (`new`) or switches (`use`) a track — rebinding the context so the vault subtree + memory sub-scope follow — or lists them, fail-closed without an active project | 12 |
| **Project mint (`/project new`)** — the guided-setup mint: the `new <name> [--path]` parser (name/quoted-path/usage; a legacy `--type` is tolerated + ignored) and the mint pipeline that registers a fresh isolated project (root `<cwd>/<slug>` or `--path`, minted `mem_ns`, made active), binds the engine to it, and seeds the first `software` vault unit via the initiative machinery; fail-closed on a duplicate root (registers before it creates the root dir, so no orphan), a name with no slug-able characters, and a bare/empty `--path` flag; the mint activates through the real quiesced switch (conversation reset, no bleed) and requires a session; the seeded unit uses a canonical `main` slug, not the project name, so the vault doc path never double-nests | 17 |
| **Project delete / archive** — the registry-mediated lifecycle verbs: `set_archived` (registry); `/project delete <id> [--purge]` (forgets every track memory scope, removes the registry entry, leaves the dirs unless `--purge`, which is itself guarded against the cwd/boot/home/ancestors; deleting the ACTIVE project switches to default first; default never deletable); `/project archive|unarchive` (reversible flag, refuses the active/default project, hidden from `list` unless `--all`); and `_switch` refusing an archived target; plus the atomic `Registry.remove(expected_root=)` (no purge on a re-registered root) | 23 |
| **Export no-project-artifacts guard** — the export invariant: the publish export carries NO runtime project state — the export guard's artifact scan asserts no `.ironclad/` machinery, `.tracks/` vault subtrees, or `registry.json` reach the staged tree (a fail-closed export gate + a backstop to the copy-ignore patterns) | 5 |
| **Self-hosting release staging** — the staged-release guards for the self-modification bootstrap: `release_preflight.staging_route_ok` (a release must route through Test-PyPI first, never straight to production) + `main_safe_reasons` (refuse a release while a rollback is pending but main HEAD is not yet reverted, so a bad build is never re-shipped), and the local-rollback wheel cache (`rollback.wheel_version`/`last_good_wheel`/`rollback_pip_argv` — pick the newest cached wheel strictly older than the bad one + a `--no-index --force-reinstall` argv, so the running instance is recovered from a local cache without touching PyPI); plus the deterministic **`changelog_cut`** tool — a pure `[Unreleased]` → `## [X.Y.Z]` release cut that opens a fresh empty `[Unreleased]` and satisfies `release_preflight`'s release-ready state, fail-closed on an empty / absent / already-cut section, so a release never depends on fragile string-manipulation of the long section; and the complete **`release_prep`** tool — a single deterministic call that bumps the version across every file that carries it (`pyproject.toml`, the private dev-loop config's version line, `docs/status.md`, `README.md`) and cuts the CHANGELOG, so a release satisfies every version-consistency invariant (the dangling-reference and status-version consistency guards), fail-closed on an absent pattern. Pure/offline | 15 |
| **Export test-drift guard** — the export-drift lint: a fail-closed export gate + a suite test flagging any exported `test_*.py` that references the private `scripts/` tree without an absence guard (`.is_file()`/`.exists()`/`pytest.skip`) — such a test `FileNotFoundError`s in the public clean-room where `scripts/` is absent; covers the clean real tree, a synthetic unguarded reference, the is_file / skipif clears, and a no-reference no-op | 5 |
| **Self-dogfood isolation acceptance** — the offline isolation acceptance: drives the real `/project new -> /switch -> stage a unit -> switch back` cycle for two projects through the quiesced switch (no live infra / model / deliver) and asserts the whole-epic invariant — distinct non-base memory partitions, vault + state machinery under each project’s own root, work artifacts only under the active project, the base/default project untouched, and no cross-project conversation bleed | 5 |
| **Lifecycle DELIVER-leg gate** — the wiring of the evidence primitives into a functioning gate: the pure `lifecycle_projector` maps dev-process **ledger** transitions to lifecycle stages (a green composed-gate leg → `tests`, a review-evidence leg → `reviews`, a `delivered*` DELIVER record → `delivery`) and composes `project_evidence` bound to the delivery `tree_sha` (deterministic + idempotent — no new files on re-projection); the `/lifecycle gate` engine command reads `<repo>/.devloop/ledger.jsonl` as plain data (hash chain **re-verified engine-side**, boundary-clean — no private dev-process import), projects + runs `lifecycle_completeness`, and reports `READY`/`BLOCKED` **fail-closed** (default `--stages delivery` — the conservative default; `tests`/`reviews` are now logged by the driver — the `log` seam is wired to `ledger.append` in run.py / `build_real_ops` — and enforced via `--stages tests,reviews,delivery`). Covers the transition→stage mapper (every real shape + None cases, incl. a dry-run/**inert** review excluded from `reviews`), projection with fakes AND the real primitives, idempotency, fail-closed (empty tree_sha / missing stage / no slug / tampered / missing ledger), the command end-to-end, the engine-to-private-ledger hash cross-check, and the producer contract (driver `log` → a chain-intact ledger carrying the GATE/REVIEW transitions) | 43 |
| **Base-untouched reconciler** — the delivered-state check: a full project lifecycle (mint/switch/stage/delete) under a throwaway workdir leaves the engine’s own source surface (`core/skills` + `engine/prompts`) **byte-identical** (a content-hash snapshot before/after, bytecode caches excluded), catching any path that resolves into the engine tree instead of the project root | 2 |
| **Read-only Memory MCP** — a dependency-free stdio JSON-RPC server exposing project memory as read-only search + deep-query tools, launched when a memory service is configured and the agent ships an `mcp_template` | 11 |
| **Open plugin surface** — discover and expose `skills/*` plugins with no core patch; plus a hung synchronous plugin tool can no longer hold the turn + agent lock forever — its run() executes in a daemon worker that observes the turn cancel + idle-watchdog budget so a blocking handler is abandoned and the turn releases the lock | 10 |
| **Extension SDK** — the curated public `ack.sdk` surface (contract, re-export identity, gate / schema / assemble) | 7 |
| **Packaged-plugin loading** — the `ironclad.plugins` entry point (root resolution, additive load, fail-soft) | 10 |
| **Export-leak guard** — internal artifacts kept out of the boundary and the public export | 4 |
| **Example plugin** — the shipped separate-repo example: discovers and runs via the loader and passes its own gate | 6 |
| **Playbook skill kind** — `SKILL.md` parse / validate / discover, progressive disclosure | 15 |
| **Skill generator** — spec → scaffold both skill kinds, schema-valid by construction (incl. free-text with quotes/backslashes/newlines, and a param set that cannot form a valid Python `run(...)` signature -- a reserved keyword name or a duplicate name is rejected up front rather than rendered into an uncompilable module that discovery silently drops; soft keywords like `match`/`type` stay valid; and `write_scaffold` preflights the COMPLETE target set before writing anything, so a conflict on a later target refuses atomically instead of leaving a half-scaffolded destination -- a discoverable skill without its generated test) | 13 |
| **Paved-road generator** — `ack.generator` template-tree render + re-runnable 3-way merge + transactional multi-file/state commit (decide all targets first; rollback prior bytes/new files on any target or state write failure) + the built-in collision guard (a generated per-project item may not shadow a core built-in), plus the engine `/generate` seam that renders into the **active project's library** (ctx-resolved `vault/library`, never `core/skills`) with the built-in set injected as the guard (output_root + reserved set engine-enforced; bad-input handled as a clean error), plus the `--kind {case,prompt}` selector (default `case` byte-identical; `prompt` renders the `new-prompt` tree into a gate-valid `kind: prompt` item with a `locales/<lang>.json` overlay, and the prompt path widens the guard to also cover built-in prompt capabilities), plus **placeholder serialization escaping** — an optional `{{ key | tojson }}` filter (the Jinja2 built-in name, so the tree stays Copier-renderable) emits a fully double-quoted scalar that is a valid Python string literal, JSON string, and YAML scalar at once, so an operator `--description` carrying quotes/backslashes/newlines renders a syntactically valid Python skill, JSON MAPPING row, and YAML scalar instead of a broken scaffold committed with exit 0 (a filterless token stays byte-identical, and the two quoted sinks stay byte-identical for an ordinary value) | 41 |
| **Skill library catalogue** — manifest index, semver, provenance, atomic stage-and-swap install / update with rollback (backup-lock-safe, discovery skips hidden staging leftovers) | 13 |
| **Skill registration gate** — doctor-preflight / schema-check / eval-gate, no unchecked code; the **generation-completeness gate** (`gate_generated`) additionally rejects an unfilled scaffold (the `ACK-SCAFFOLD-SENTINEL` marker) + **hermetic sibling-test execution** (`run_sibling_test_hermetic`: scrubbed env / hard timeout / isolated tmp; opt-in `gate_generated(execute=True)`); a **strict-locale variant** of the prompt gate (`gate_prompt(strict_locales=True)` — a declared non-source language whose `locales/<lang>.json` overlay is absent is a failure: "declared == delivered"; lenient fallback stays the default); and the **library-completeness invariant** (`library_items_complete` — gate every generated tool **and** every generated `kind: prompt` item [strict locales] in a per-project library; for isolation acceptance / operator); plus opt-in per-task `--validate-tasks` over the real base task records (DOCTOR-VAL) — now validating a capability task record too (against `CapabilityTaskSpec` when Lodestar is enabled, else the base `TaskSpec`), never silently skipping it; plus **fail-closed Lodestar loading** — when `--lodestar` (or the enabled engine path) is requested but the plugin cannot be imported/initialised, the doctor emits a visible ERROR instead of silently running only the generic checks and reporting success | 48 |
| **Skill lifecycle end-to-end** — generate → gate → register → load → invoke | 3 |
| **Shared content i18n** — the `ack.i18n` overlay loader (parameterized dir, fallback) | 7 |
| **Core built-in loader** — always-on built-ins from a fixed dir; plugins additive | 4 |
| **MPR core built-in** — router / registry / synthesis / audit / panels / templates / eval / packaging + optional `artifact_slug` port for initiative-bound `runs_dir`; the reserved `prune_runs` retention utility protects violated and unreadable-provenance evidence by default and reports/index-deletes only verified directory removals | 396 |
| **Prompt-library item** — `kind: prompt` parse / validate / discover + variable build | 7 |
| **Multilingual prompt assembly** — template + values → target language | 6 |
| **Prompt slash surface & elicitation** — list → guided ask-next → assemble + language | 9 |
| **Curated prompt library** — the shipped starter prompts: discover + gate + assemble (English + German) | 33 |
| **Discovery commands** — `/prompts` and `/skills` list the one loaded registry | 6 |
| **Per-item prompt invocation** — `/<prompt-name>` resolve + parse + elicit / assemble | 14 |
| **Orchestration state** — task lifecycle / dedup, initiative (one type: `software` — MPR is embedded, not a project type), autoplan, the model-invokable launch_coder trigger verb, the always-on fail-closed design→impl approval gate + non-destructive `record_design`/`/approve` proposal→decision lifecycle (no blind coding) and the post-research `record_design NOW` steering cue, the GitHub-issue-shaped unit data model (labels + parent for 1:1 (sub-)issue mapping), the central task board (BOARD.md /board — LLM-free projection of the pipeline), always-on fail-closed completion authority (selected feedback must be readable, non-empty, and explicitly normalized to `status: done`), the watcher/autopilot disentangle + task heartbeat + explicit blocked state (opt-in automation.decoupled/heartbeat.stall_seconds), the code-subdir software isolation (opt-in paths.code_subdir), the markdown-safe project/initiative/track list markers, autonomous-mode empty-pipeline hint, navigable design-doc path, the dev-loop stabilization (tolerant completion-status parse `_feedback_status` incl. a bare leading line + ingest-stamp, mtime-keyed advance retry, task_id-glob feedback match with filename-derived agent, canonical routed-agent stamp), the design→epic decomposition plan_units (one epic + parent-linked handover-less units; atomic, deduped incl. in-batch, unit:<n> sibling deps, epic_id append, done-epic refusal), the deterministic select-next-unit continuation (_continuation_tick legs units→backlog→idle-armed, work-in-flight gate, deadlock surfacing, no self-disable) + epic auto-complete on the last child's advance, the /auto automation meta-switch + guided-mode next-unit recommendation (steering state + advance result), watcher default-off and `/watcher` compatibility alias coverage, re-hand staging parity (id normalization + assigned_to stamp), the arming bootstrap kick, the /auto-owns-launching defer so launch_coder never double-drives the loop yet still launches in the autopilot-only/autoplan-only mixed states and still surfaces an unknown-agent config error, the plan_units no-sibling-order build-order steering note + foundational-unit bootstrap/guided recommendation, the handover body-Recipient normalization to the resolved agent, the configured-code-root handover note, L1 constraint capture (`record_constraints`, canonical decision, strict tri-state reader, default-off tool/steering/dispatch, reserved-marker refusal, and malformed-document fail-closed cases), optional framing-note capture plus unconditional approved-design-standard injection (framing can be off without disabling design protection), the L1 real-tool-dispatch E2E capstone + gate-off byte-identical, the typed constraint fields + hard/soft classifier (ADR-0016), the L2 structured conflict detector + durable fork-envelope emission at `record_design` under `proposals/forks/` when `safety.constraint_conflict_detect` is on, and the L2 durable project-scoped fork worker + `/fork` surface + decide→learn (non-durable process-local run lock, submit-time `copy_context`/`ctx.run`, opaque `/fork list`/`decide`, fail-closed `/approve design` on pending or ledger error when detect is on, decide→learn; both flags off byte-identical), the always-on language-only typed hard-check at impl `stage_handover`/`plan_units` (pure hardcheck/Violation, omission+mismatch refuse, force no-bypass; design network remains separate), the L2/L3 real-dispatch E2E capstone + gate-off byte-identical (keep refuse-until-rerecord, counter override-and-proceed, omission fail-closed; MPR not required), the capture-completeness gate (a best-effort presence-only DE+EN body detector `body_states_typed_constraint` returning which typed categories (language/network) the prose states; when the gate is on and a constraint is not declared-none, `record_constraints` fail-closed refuses a capture whose prose states a category but omits its typed field, naming the category (not a value) so a stated floor can never be silently lost to prose; network detection is restrictive-only (offline/none/forbidden — the permissive `allowed`/`online` default is not gated) with symmetric DE+EN negation→network bridges, and language detection needs an explicit key / `only` / `must` / `requires` / `use` / implementation-verb qualifier so bare `in/using <lang>` and ordinary prose never trip a spurious refusal), state end-to-end; plus the `/design --options [N]` proposals-with-trade-offs command (ADR-0006); plus the `_design_egress_policy` reader (ADR-0008 — optional build-policy egress lines `network`/`allow`/`deny`, no resurrected typed-compare) and the always-on fail-closed advance-time egress boundary driven solely by the approved design `## Build policy` network posture (`network: none`/`declared` runs analyzers against the produced code root and refuses on a dependency-closure/sandbox block finding, an unavailable code root, or an analyzer/internal error; `network: open` or no `## Build policy` section advances without analyzers; the retired `security.egress_analysis.enabled` / `GX10_EGRESS_ANALYSIS_ENABLED` controls are warning-only tombstones) ; plus renewable client-run task claims (POST `/claim` pending→in_progress stamps a lease and in_progress→in_progress renews it; expired unblocked claims return to pending; `/unclaim` reverts immediately) | 308 |
| **Project registry & isolation** — the installation-global project registry: atomic SSOT round-trip, a cross-process file lock (stale reclaim), >=64-bit mem_ns minting, the ADR-0007 reconciler (re-mint duplicates / quarantine missing roots / rebuild-from-disk), the implicit default project, and per-slug legacy migration, plus the request-scoped ProjectContext (contextvar resolver + copy_context thread-binding) routing the path roots, plus memory & warm scoping by the active mem_ns (ctx-aware _ids + scoped warm session + namespaced retrieval cache + ctx-bound generation workers + scoped MCP launch, plus the AST CI-lint forbidding raw scope-bearing globals outside the accessors; qualified-scope + cross-module from-import/getattr; scope-correct decorators/defaults; fully-qualified gx10 path + import-as alias + star-import + from-pkg-import-module), plus the project-overlay discipline (locked-key denylist + closed-allowlist + path-containment validator; parent-replacement bypass guards), plus the quiesced project-switch core (refuse-in-flight, save-leaving/load-entering session, ctx rebind, locked-config rebuild, save-leaving-then-load-entering ordering, rollback-on-failure, ctx_for-injection), plus the registry<->engine wiring (default binds the base partition behaviour-preserving, a registered project isolates, switch-back restores base; default re-points to the boot workdir; corrupt active-pointer repaired; init-failure clears ctx; default binds the boot workdir not a shared stored root; bind uses the per-process active cache; /switch + /project verbs end-to-end — no conversation bleed, leaving saved under its own root, in-flight refusal, cache updated after commit; corrupt-target no-bleed; rolling-summary dropped; last_response cleared; failed-save aborts; set_active-failure rollback), plus the **exec-cwd seam** (the model's file tools + `execute_command` + the launched code-agent resolve against the active project's root — `_exec_cwd`/`_resolve_exec_path`; absolute paths verbatim; default == the boot workdir byte-identical; no-context unchanged; empty-source move refused), plus the **code-agent memory scoping** (the launched code-agent's read-only Memory MCP env namespace + the worker / MPR reducer mirror write resolve to the active project's `mem_ns`; default partition byte-identical), plus the **project-scoped loader** (the active project's `library/` is discovered alongside built-ins — last/additive + cross-root capability-guarded, byte-identical when absent; reloaded on `/switch` via build-then-swap; an unfilled scaffold is dropped at load), plus the **per-track vault subtree** (a non-`main` active track is isolated under a hidden `.tracks/<track>/` subtree of the project vault — `_active_track` + track-aware `vault_root`; the default `main` track and no-context are byte-identical to the pre-track layout; an unsafe track id falls back to `main`), plus the **per-project + per-track vault-mutation lock** (a `vault_lock(pid, track)` distinct from the dev-loop `project_lock`, born-wired around the vault writers `initiative_new`/`reconcile_vault` — reentrant within a call stack so nested writers never self-deadlock, OS-serialized cross-process/thread, fail-soft when locking is unavailable; different tracks don't contend; the dev-pipeline macros `_stage_handover`/`_advance_pipeline` also run under it), plus the **typed-edge vault graph** (typed frontmatter edges [`depends_on`/`refines`/`supersedes`/`relates_to`/`implements`/`blocks`] → a deterministic, LLM-free machine SSOT `GRAPH.json` with full-relpath node keys [no timestamp ⇒ idempotent] + a human `LIFECYCLE.md` view, both generated next to `INDEX.md`; an unresolvable edge target is flagged `dangling`, not dropped; the generated files are excluded from the doc scan and the HTML markers stay frozen; aliased edge targets that resolve to the same doc collapse to one edge), plus the **composable lifecycle stages** (an ordered stage model `idea→design→adr→spec→tests→proposals→reviews→delivery`; a doc declares its `stage` in frontmatter and the initiative's lifecycle is COMPOSED via `lifecycle_state` [present / current / gaps / complete / counts / unknown], surfaced in `GRAPH.json` + `LIFECYCLE.md`; `can_advance_stage` is the fail-closed transition guard — forward-only unless `allow_regress`, unknown stages refused — the primitive the `/lifecycle` command + the DELIVER completeness gate build on), plus **project-scoped + cross-track reconcile** (`reconcile_active_project` reconciles every initiative in the current track, and `reconcile_all_tracks` is the scheduled cross-track reconciler — it sweeps every track of the project [`main` + `.tracks/*`], **fail-closed per track**, idempotent; wired as `/initiative reconcile all`), plus **i18n of the vault/initiative chrome** (the German prose written into the generated vault docs [INDEX/LIFECYCLE] + the reconcile/initiative/mpr messages now route through `engine/messages.py` — English source/default [public-grade export], German overlay, language = `gx10.LANGUAGE`; the `ironclad:*:auto` HTML markers stay frozen and are never localized), plus the **evidence projector + lifecycle-completeness gate** (`project_evidence(stage, title, body, *, tree_sha, content_hash)` appends a `type: evidence` stage-tagged doc bound to `tree_sha`+`content_hash` under `<slug>/evidence/`, deterministic filename + idempotent + append-only [never rewrites curated bodies]; `lifecycle_completeness(slug, *, required_stages, tree_sha)` is the fully-fail-closed DELIVER-leg gate — every required stage present AND bound to the delivery tree_sha; tree_sha/content_hash hex-validated [no filename traversal]; CRLF-normalized for idempotency), plus the **per-track memory sub-scope** (`ProjectContext.mem_scope()` composes `<mem_ns>::track::<tid>` for a non-`main` track — flowing through both the cold partition `MemoryManager._ids()` and the warm session/cache namespace; `main`/no-ctx/empty-ns byte-identical; an unsafe track falls back to `main`, matching the vault subtree; the vault + memory track-safety predicates agree for every input incl. non-str), plus the **cross-switch memory re-keying lock** (a single build-once `MemoryManager`/warm handle follows the active scope **per call** — `/switch` re-keys via the context, no rebuild — proven across A→B→A switches + the track dimension + registered-project-never-base; the installation-global, overlay-locked connection makes a "rebuild-on-switch factory" unnecessary), plus the **per-principal RBAC + multi-tenant foundation** (`ack.authz` — a `Principal` id/role/tenant, a deny-by-default role→danger-tier policy, token→principal resolution, and `tenant_scope` isolating memory by tenant [`default`=no-op]; the engine `_authorize_action`/`_tenant_mem_scope` gated on `security.multi_tenant` default-off, byte-identical), plus the bridged-tool exec-cwd override so the orchestrator's OWN local-tools run in the active project not the boot workdir | 262 |
| **Dev-process public facade** — the curated, versioned `ack.devprocess.api` seam: import + version, graceful degrade (every verb raises `SubstrateUnavailable` with no driver), the `runtime_checkable` driver protocol, set/get-driver registration, the five verbs delegating to a registered driver, the optional-task_id create-new shape, the required deliver ledger_path, and the curated `__all__` surface, plus the in-engine driver wiring (registered at gx10 import; `stage_handover`/`advance` delegate to the real impls late-bound; `select_unit`/`deliver`/`record_feedback` report unavailable; the stage_handover/advance_pipeline tool dispatch routed through the facade, with a direct-impl fallback when no driver; the real-launch import-order (core/engine-only on sys.path) resolves the late facade import; registration never clobbers a richer pre-registered driver; plus the pre-publish-orchestrator surface proof mirrored as a counted test — the wheel carries the orchestration verbs + fail-closes without a driver, in lock-step with the clean-room `Orchestrator-surface proof` step) | 17 |
| **Parallelism** — governed fan-out, the in-engine tool, single-writer reduce, the parallel router | 29 |
| **Thin client + BYO code-agent** — the agent pool, a configurable agent-command template, managed transport, renewable claim leases while a coder runs, the config-driven code-agent registry, a per-agent boot probe, result classification, onboarded-but-disabled agents, and launch-model resolution (a handover `to:` naming the agent is the recipient, not a `--model` override; plus the engine-stated exact feedback-path + `status:` contract in the Claude coder prompt so a completed run advances the pipeline; plus deterministic cost-tier routing to the cheapest capable coder per tier; plus the coder permission default that lets it run its own tests; plus line-drained server-side coder logs that are visible before child exit for complete lines, retain flushed partial output after a killed child, and survive invalid stdout bytes without hanging the child) | 95 |
| **Runtime-aware output & language** — encoding safety, color gating, reply language, streaming `<think>`/`<tool_call>` render suppression | 20 |
| **Token budget / context trimming** — token-accurate budgeting, a pre-flight overflow guard with emergency trim, the char-fallback watermark reserving sys+tools+thinking, and live context-length discovery, plus the **budget-aware read cap + emergency-trim recovery**: `read_file` caps to the live per-turn window budget (a contextvar cap = window − reserve − transcript, ×0.8, floored) so a single read can't overflow — falling soft to the fixed ceiling without an exact tokenizer — and `_emergency_trim` TRUNCATES an irreducible single oversized turn (marker) instead of raising | 65 |
| **Misc** — manual cat tool, orchestrator version, runtime self-telemetry (rolling latency/error/cost + SLO/anomaly + `/metrics`), the alerting pipeline (SLO/anomaly → deploy-correlated alerts + outbound webhook page + inbound `POST /alert` receiver) | 21 |
| **Demo vessel** — the example-workspace doctor preflight | 1 |
| **Documentation & release integrity (internal QA)** — documentation-reality checks, the generated roadmap and test counts, export-sync verification, the clean-room pre-publish proof, deploy-consistency checks, run-artifact retention (`prune_runs`), memory/state backup retention (`backup_retention`), the operate-phase scheduler primitive (`scheduler`), and the maintainers' release-process guards | 90 |
| **Command-spec & UX foundation** — the hand-authored command-spec (a parallel description of the slash-command surface: verbs, flags, danger-tiers) and the spec↔dispatch parity guard that derives the verb set from `_dispatch` source and fails on drift, so the friendly-UX layers can be derived from one source without the executor ever becoming spec-driven; the spec is served via `GET /catalogue` and the client generates its server-command completions from it (static list as the cold-start fallback); plus spec-derived discovery — `/config keys` (with an unknown-root config-set guard), tool-parameter surfacing on `/skills`, and a danger-tier-grouped `/help`; and a `--tree` resolver defaulting `/lifecycle gate` to the committed git HEAD tree (fail-soft, explicit `--tree` overrides); and **alias / unambiguous-non-destructive-prefix / did-you-mean** resolution in both clients' `classify()` (zero-cost + deterministic — a typo becomes a suggestion, not a billed turn; resolves to the canonical command re-parsed through the untouched dispatch); and a **server-side confirm-before-execute contract** for destructive ops (`/project delete` returns a `needs_confirm` reply, uniform across clients, re-run with `--yes`); a spec-derived `guided_usage` + structured `flags`/`subcommands` in the `/catalogue` entries (single-source guidance + client autocomplete); and `/ace` defaulting its `--ledger` + a plain-language `eval` verdict (jargon out of the hot path); and **i18n** of the new engine-chrome strings (the confirm reason + `/ace` verdict via `_msg`/`messages.py`, EN source + DE overlay) plus a public **command-ergonomics** doc; and two fail-closed drift guards — a private ink-command-parity check (the ink ALIASES/UNSAFE mirror == the command-spec) and a private ink-test-count check (the documented TS counts == the ink suite); and the ink static command mirror completed + coverage-guarded so the did-you-mean net covers every spec verb; and spec-derived `guided_usage` wired into every worst-offender (/config set, /ace, /project, /generate) so their usage is single-source, not hand-written; and a server-side structured **needs_guidance** contract (`_guide_required` — on an explicit `?`/`--guide` it returns the fields/choices/defaults/usage for a verb, uniform across clients, `_dispatch` untouched); and every client (ink field-by-field + the 3 Python REPLs via a shared `render_guide`) renders that `needs_guide` reply, so `/verb ?` shows the required fields/choices/defaults instead of a billed turn; and the remaining engine chrome localized via `_msg` (config-keys header/values+types, danger-tier `/help`, unknown-root refusal, `/skills` params) with a single-language destructive-confirm line; the confirm + needs_guide gates fire on the slash-stripped client wire form (bugfix — they had required a leading `/` that clients strip, so both contracts were dead end-to-end); and the guidance now teaches the primary `/project` (model system prompt + client/server `/help` + fail-closed messages + docs), not the deprecated `/initiative` alias (bugfix); and the exported orchestrator system prompt + the engine/ink user-facing strings are English (the German prompt moved to a private deployment override); the MPR skill is English; and a fail-closed English-only export guard keeps the export German-free; and the orchestrator is injected a spec-derived command digest (canonical/deprecated/danger) so it never misnames a command | 57 |
| **Config control plane** — the typed `config_schema` SSOT (every effective leaf's exact type/default, optional enum/range/cross-leaf relationship, boot-only vs runtime lifecycle, switch/tuning class, secret/redaction policy, env boundary parser, and deprecation/tombstone metadata), the schema-derived `_code_defaults` + `_FROZEN_CONFIG_KEYS` + `command_spec.SPEC_FROZEN_CONFIG_KEYS` three-way parity, strict file/dict boolean rejection + one documented env-bool parser, the plugin-root-tolerant `validate()` of the complete merged projection, the tolerant memory/warm external-seam file merge with endpoint-gated activation, `setup.type` incl. the `auto` install value, and the transactional `/config set` (deep-clone → validate → derive full unpublished state → commit globals + reversible hook/store/worker wiring under the config lock → publish, or a complete snapshot rollback that restores every derived+reconfiguration global, hooks, stores, and newly-started workers with exactly one red refusal and no green). It also adds a byte-exact generated public reference plus a non-vacuous AST guard proving schema switches = documented switches = operational reads, rejecting tombstones/unknown raw boolean gates, and independently checking the complete boot-only inventory. | 80 |
| **Secure deployment defaults** — the operator-ratified secure fresh-install posture: loopback-only default bind with a named, greppable `security.allow_unauthenticated_bind` override (a non-loopback `open` bind refuses fail-closed at boot without it or a token/sealed profile); the removed permission-bypass defaults (coder launches `permission_mode: default`, and `--dangerously-skip-permissions`/`bypassPermissions` is restored only by an explicit per-agent `capabilities.permission_bypass`, still bounded by tooling authorization + sandbox); `search.enabled`/`forge.enabled` default OFF; finite timeout defaults + hard ceilings (connect/first-token/provider-CLI/concurrency/autoplan) with `0=unlimited` refused for autonomous external writers; and the fresh-installs-only migration that preserves an existing private deployment byte-for-byte via explicit configuration | 6 |
| **Config docs \& switch parity** — a deterministic private CI generator that emits every effective leaf's type/default/env/lifecycle/bounds/switch-class/tombstone and the schema-owned memory/warm external seams into marked regions of `config-runtime.md` (`--check` fails on byte drift); and the switch-parity guard that proves schema-switches == documented-switches == read-switches, refuses raw operational config gates outside the typed accessor, resolves IfExp/walrus aliases, module-dict/comprehension/lambda gates, and `getattr`/`itemgetter`/call-rooted/string-constant/walrus-key evasions, catches tombstone reads before rebinds, at non-gate call-rooted sites, and inside display-only helpers, checks seam/schema drift, and asserts the schema boot-only set equals `_FROZEN_CONFIG_KEYS` and `command_spec.SPEC_FROZEN_CONFIG_KEYS` | 34 |
| **Full-Ironclad audit fixes** -- logic+design defects surfaced by the post-audit review and verified code-real: playbook reference containment is now physical (anchored to the resolved playbook dir; a symlinked reference OR a symlinked/junctioned references/ dir is refused, the resolved target must stay under the playbook, the resolved path is read to close a check-then-read TOCTOU); and the model-facing filesystem tools (read_file / list_directory / search_files, Python + ink) are bounded so they never allocate/decode unbounded data before capping (OOM protection: a stat/byte ceiling refuses a multi-GB file, list stops at cap-plus-one, search short-circuits at its hit/file budget) with an honest overflow note; and Lodestar capability-tracking now resolves a `depends_on` GLOBALLY -- a capability implemented (done task) in ANY domain satisfies a cross-domain dependency, so `build_backlog` renders it as a READY open gap instead of parking it in the Blocked section forever (matching the doctor's cross-mapping resolution); and per-turn session persistence is atomic — `save_session` writes a fsync'd temp file and `os.replace()`s it into `session.json`, so a failed/crashed write (ENOSPC, kill) leaves the previous valid checkpoint intact instead of a truncated/partial file that `load_session` cannot parse; and session RELOAD retains the generated rolling-summary system message (marker-prefixed) that holds evicted history instead of filtering out every system message by role, so a long conversation's continuity survives a restart with no warm tier | 11 |
| **Internal dev-process injection** — the per-project INJECTION descriptor that marks a project as running the INTERNAL (extension-driven) dev process, SEPARATE from the delivery target table: the engine reads it as PLAIN DATA (`_dev_target_descriptor` — no import of the private dev-loop machinery, the tool↔process edge stays data-only) and the `/lifecycle` gate fail-closed-reconciles it against the project registry (`_dev_target_drift` — a descriptor naming an unregistered project BLOCKS). The pure schema + validators (`devprocess.spec.validate_injection`/`injection_drift`) and the runtime side-file storage + atomic registry-locked bind + reconcile CLI are the private substrate (not in the wheel); the fail-closed run decision (`plugin_active` health probe + `entry_plan` refuse + `devtarget.decide`, private) refuses the internal driver when the plugin is inactive; and the MUTUAL EXCLUSION — the normal in-engine pipeline (`_internal_target_blocks_normal` at stage_handover/advance/TaskStore) is OFF on an internal target, `/switch` serializes under a repo-global lock, and `/fork`+`/lifecycle` scan records are scoped per-project mem_ns (no cross-project bleed); ledger records are stamped with project_id+exec_mode + a fork is refused on resume; and `/status` surfaces the injection mode (internal-target exec_mode/tier/plugin vs normal) | 11 |
| **Live smoke** — real model, all endpoints | 9 |

## Live end-to-end verification

Run against a real deployment — a DGX Spark (GB10) serving **Qwen3.6-35B-A3B-NVFP4** via
vLLM with the orchestrator and Mem0 memory stack co-located, driven from a workstation
over the LAN.

**Orchestrator HTTP surface (live smoke, 9/9):** health, a simple chat turn, a
**tool-using** turn (the model calls `list_directory` and answers from it), streaming,
the task snapshot, governed fan-out (concurrent, measured speedup), input validation
(`/fanout` rejects empty), cancel, and a memory-backed turn (`query_memory`).

**The full agentic loop — the headline flow — end to end:**

1. A chat turn makes the orchestrator plan and `stage_handover` a task (its `task_json`
   validated through the ACK contract gate) → a task is created and the handover staged.
2. The thin client pulls `/pending`, claims the task through `/claim` so it shows as
   **in progress**, runs the local code-agent against a local working copy, and uploads
   the result via `/feedback`. A failed run releases the task through `/unclaim` for retry.
3. The server's reconciler advances the task → **done**.

This was verified **twice**: once with a deterministic stub agent (proves the
client↔server↔reconciler contract repeatably), and once with a **real `claude --print`
code-agent** that actually created the file in the local repo, wrote its feedback, and
drove the task to **done** — proving the advertised "code stays on your machine, the
code-agent runs there" flow with the real binary.

**Sealed channel:** separately verified over a real SSH tunnel — loopback-only
bind, gated routes refuse without a session, a real model turn through the sealed channel,
re-seal on disconnect (details in [`status.md`](status.md)).

## Issues found during this campaign — and fixed

Transparency over polish: the full test deliberately exercised the real path, and it
caught a real gap.

- **Headless code-agent could not write files.** The thin client launched `claude
  --print` with **no permission mode**, so the local code-agent had no way to approve
  file writes in headless operation — it exited having done nothing. The orchestration
  plumbing was fine, but the advertised "edit local code" flow was broken for the real
  binary. **Fixed:** the client now passes `--permission-mode` (default `bypassPermissions`
  so the coder can also run the tests it writes, configurable via
  `GX10_CLAUDE_PERMISSION_MODE`); re-verified end to end with a real code-agent. A
  regression test pins the flag.

Earlier in the same hardening pass, a smaller gap was also fixed: `/tasks` was readable
without the deployment secret under the auth profiles (now gated), and a flaky
socket-timing assertion in the tunnel test was made deterministic.

**Adversarial review of the new code.** Two independent reviewers audited the full diff
of this work and surfaced a handful of real issues, all fixed and regression-tested:

- **(high) Model-agnostic tool-call recovery could hijack a JSON answer.** The recovery
  path that reads tool calls from *text* (for models without native tool-calls) had a
  branch that fired on any bare top-level JSON object whose `name` matched a tool — so a
  legitimate answer that happened to be JSON (or an echoed tool spec) could be silently
  re-interpreted into a **destructive** call (`write_file`/`execute_command`/`delete_file`).
  Fixed: only **explicit** `<tool_call>` tags and fenced blocks are recovered now; a bare
  object is never treated as a call.
- **(med) Auth-gate / router path mismatch.** The gate and the router compared the raw
  path; a query string could desync them. Both now normalize to the query-free path.
- **(med) Request-body cap.** `Content-Length` is now capped (8 MiB) to bound per-
  connection allocation on the threaded server.
- **(med) Orphaned tunnel child.** If the client-managed tunnel failed to come up, its
  subprocess was not torn down (the context manager's exit doesn't run when entry
  raises). It is now reaped on any failure.
- **(med) Config-tree slurp.** Directory config descent now skips hidden/dotted subdirs
  (no `.git`/`.vscode` pickup).

## Honest limitations

- The live suite needs a running endpoint; it is **skipped** in plain CI.
- The real code-agent step depends on a local `claude` binary and a permission mode that
  allows unattended work; the coder now defaults to the safe `permission_mode: default`
  (edits proposable, **no** command execution), so running the tests it writes requires opting the
  individual agent into `permission_mode: bypassPermissions` + `capabilities.permission_bypass: true`.
  Understand the implication (it then runs commands on your own machine, against your own code,
  session-gated, still bounded by tooling authorization + the OS sandbox).
- Multi-user identity/authorization is **not** built (single-tenant by design — see
  [`roadmap.md`](roadmap.md)); nothing here tests multi-tenant isolation because there
  is none yet.
- `main` is a development snapshot. These results reflect that snapshot, not a release.
