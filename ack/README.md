# core/ack/ — Agent-Contract-Kernel

The consolidated ACK: Case-Spec (Pydantic-v2 SSOT), Validated-Emit (soft path:
validate → reask → retry, with an injected chat transport), Constrained-Emission
(the parked grammar hard-floor), Registry (discovery), Doctor (workspace preflight),
Generator (paved-road scaffolding), plus the opt-in **Lodestar** capability/gap
plugin (`lodestar/`). Secret-free and standalone — enforced by the boundary check.
